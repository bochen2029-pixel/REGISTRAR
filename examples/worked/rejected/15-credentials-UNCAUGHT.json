{
  "$comment": "FIXTURE 15 — FICTIONAL — AND IT IS NOT REFUSED. THE LARGEST BLAST RADIUS FOUND. `allocation.credentials` IS a declared L3 target, so blast radius is satisfied. This row repoints the national allocation system at an arbitrary host, ships a live-looking API key in a file destined for the site's version control, and disables TLS verification. All thirteen gates report content. The seed declares WHERE a value may go and never WHAT SHAPE it must have — `targets.json` carries `layer`, `state` and `note`, and no type at all. A NEAR-MISS WORTH RECORDING: an earlier draft used the key `sk-live-8f2b1`, and divergence DID refuse it — on the digit 8, as an unaccounted quantity. Removing the digit removes the refusal. That was tokenisation noise, not a security check, and mistaking one for the other is how a battery is trusted for something it never did.",
  "$status": "UNCAUGHT — retained as an open exposure",
  "$found_by": "Fork C adversarial sweep, 2026-08-26; verified by hand",
  "schema_version": "0.1",
  "site": {
    "id": "northlake",
    "name": "Northlake Donor Services (FICTIONAL — no real data)"
  },
  "rows": [
    {
      "target": "allocation.credentials",
      "value": {
        "endpoint": "https://match-mirror.example.net/api",
        "api_key": "sk-live-fdbeacfdbeac",
        "verify_tls": false
      },
      "inverse": null,
      "evidence": [
        {
          "kind": "document",
          "source": "ops/integration-notes.md",
          "says": "the match endpoint is reached over the production integration account"
        }
      ],
      "shadow_run": {
        "cases": 210,
        "would_have_matched": 210,
        "would_have_missed": 0,
        "window": "2025-01-01/2025-12-31"
      },
      "expiry": "2027-02-01",
      "author": "M. Okonkwo — Quality Director"
    }
  ]
}
