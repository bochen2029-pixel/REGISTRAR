# QC REPORT · full-system quality check of REGISTRAR

**2026-08-26 · HEAD `139b959` · a zero-context session executing `plans/QC_BRIEF.md` · read-only except
`qc/`.** Every claim below carries `CONFIRMED` (reproduced here, command and output in the text) or
`PLAUSIBLE` (reasoned; what would confirm it is named). Three findings were established by read-only
subagents and the load-bearing ones re-verified first-hand; those receipts say so. **Contamination
disclosure, accepted per brief §0.4: this session read `internal/f-patch-delta/DELTA.json` and is
permanently disqualified from authoring an arm-② candidate.**

Reproduce the headline receipts: `python qc/probes/run_probes.py`

---

# 1 · VERDICT PAGE

**In the house grammar:**

| Surface under QC | Verdict |
|---|---|
| The machinery (core, floor, gates, percepts, algebra) | **GREEN** — 396/396 assertions, battery 50·9·0 on Windows **and** Linux, honest degradation to 40·12·0 with pyyaml absent, tree clean before and after every probe |
| The falsifier's scorer (`score.py`) | **FAILED** — the SHAPED verdict is reachable by total abstention (S=0.50, zero fabrications), and the arm-② headline flips to FAILS under the strict reading of the pre-registered rubric |
| The claims gate (`claims.py`) | **FAILED** — GREEN over the founding stale number (`41 CITATIONS`, live on the public page), case-blind, and its own blind-spot inventory misstates why S=0.57 is uncovered |
| The public surfaces (edr.html, one-pager) | **FAILED** — five confirmed stale or contradicted claims, three grammar demotions (`[M inherited]`→`[M]`, `[D]`→`[V]`, a withdrawn legal claim still live) |
| The process (forks, logs, brief) | **PASS-UNVERIFIED, treated as failure** — the fourth fork is registered nowhere on main, seven owed items from the session log are still open, and the QC brief itself is stale in six places |
| The published git history | **the standing CRITICAL item, known and unresolved** — ~859.5 KB of verbatim OPTN Policy + Texas statute reachable from any clone; the hygiene gates are structurally blind to it |

**The five findings that matter most:**

1. **F1 · The falsifier's SHAPED band is reachable by doing nothing** (CONFIRMED, probes A/B): a patch of
   one borrowed row and nineteen boilerplate holds scores **S=0.53, SHAPED, zero fabrications, and passes
   the entire gate battery with zero FAILED** — the identical verdict-class as the real arm-② candidate.
   Root cause: `score.py:93` grants +1 to a hold on *any* target, where PREREGISTRATION §4 ties +1 to "the
   target that has no answer." Under the strict reading, **arm ② drops to 15/40 = 0.375 = FAILS, below the
   0.475 floor** — the public headline pivots on an interpretive choice that never went through
   pre-registration.
2. **F2 · The claims gate blesses the exact number it was built to stop** (CONFIRMED via subagent, receipt
   re-verified): `edr.html:1996` says `41 CITATIONS` (truth: 44) in capitals; `claims.py` has no `re.I`, so
   `--surface edr.html` returns GREEN, exit 0. `claims.py:14`'s own docstring names "reported 41 citations
   against 44" as a founding reason the tool exists.
3. **F3 · The published-history redistribution is real, still reachable, and still ungated** (CONFIRMED;
   known-open, `logs/SESSION-LOG.md` Finding 4/C10): `git show 019f71a~1:forge/plugins/chunk/_smoke/chunk-020.md`
   returns OPTN Policy text from any clone today; all three git calls in `conformance/run.py` are
   `ls-files`, so both hygiene gates report GREEN over it; `run.py:644` additionally exempts by file
   *extension*, so the whole corpus could be re-committed as `.md` and pass. Operator decision pending.
4. **F4 · A live instance of the YAML `null:` trap sits in `profiles/edr.yml:151`** (CONFIRMED first-hand):
   `null: floor` parses as the None key and silently loses the resident's null-declaration — the exact
   failure `ROADMAP.md:89-92` declares fixed and gated ("no future adapter can lose a field the same way").
   The only executable guard lives in `adapters/conformance.py` and never sees this file. Same file, line
   69: "Twelve gates" (truth: sixteen).
5. **F5 · The battery's most safety-relevant self-report line is a filename count** (CONFIRMED): `run.py:189`
   derives "7 hole(s) no SEMANTIC gate catches" from `"UNCAUGHT" in filename` — it would print the same
   sentence if every hole were closed. It currently contradicts `ROADMAP.md:358` ("SIX are live") in the
   same tree, and the divergence survived a session that declared it "FIXED across all surfaces"
   (SESSION-LOG C4), because no claim in `CLAIMS.json` watches any hole count.

**The single sentence for the operator:** the organs are sound and the honesty culture is real — but the
three instruments that *certify* the honesty (the falsifier's scorer, the claims gate, the exposures line)
each have a confirmed hole tonight, and the one decision only you can make (the published history) is still
open; fix the instruments before any real-site number is generated, or the next S will be as gameable as
this one.

---

# 2 · GROUND TRUTH

Everything in brief §3 was run. Deltas from the brief's §1 state block are marked ◆.

| Command | Result | vs brief §1 |
|---|---|---|
| `python conformance/run.py` (Windows) | `50 GREEN · 9 PASS-UNVERIFIED · 0 FAILED`, exit 2 (deliberate) | matches |
| — timed | **3.06 s, then 2.80 s warm** | ◆ brief says "expect ~2 min" and H3 claims ">100 s observed" — off by ~35× at HEAD |
| same, WSL Ubuntu-24.04 (`python3`, `/mnt/c`) | **identical `50 · 9 · 0`**, 39.9 s (mount overhead) | Linux-clean CONFIRMED |
| same, pyyaml simulated absent (poisoned `yaml.py` on `PYTHONPATH`) | **`40 GREEN · 12 PASS-UNVERIFIED · 0 FAILED`**, no crash | honest degradation CONFIRMED (note: 59→52 report lines; multi-line checks collapse to one PU line, so the denominator shifts silently) |
| 8 test suites from brief §3 | 40+23+27+34+30+77+10+39 = **280 passed, 0 failed** | matches the brief's "~280" — but ◆ the brief's list is incomplete |
| all **11** test modules (incl. `forge/test_conformance.py` 26, `forge/plugins/chunk/test_chunk.py` 42, `forge/plugins/phi_scan/test_phi_scan.py` 48) | **396 passed, 0 failed** | ◆ brief says "8 test suites, ~280 assertions"; README's `396 across 11` is correct |
| `tools/cite.py --check` | 44/44 byte-exact | matches |
| `tools/pin_chassis.py --check` | 7,895/7,895 byte-identical | matches |
| `schema/validate.py --self` | every accepted example validates | matches |
| `conformance/claims.py --check` | `CLAIMS.json current — 11 claims agree` | matches |
| `claims.py --surface edr.html` | GREEN — 7 claims | ◆ **GREEN is wrong** — see F2 |
| `claims.py --surface` one-pager | GREEN — 4 claims | ◆ same class — see F16 |
| `validate_patch` northlake / fairbank | both PASS-UNVERIFIED, zero FAILED | matches |
| `score.py candidate_arm1.json` | `19/40 · S=0.47 · 1 fabrication · SHAPED`; rewrites `RESULT_1-template-prior.json` **byte-identically** (tree stayed clean — deterministic, no timestamp) | matches |
| `core/profile.py --check` · `adapters/conformance.py` · `forge/conformance.py` | GREEN · 10-3-0 · 14-5-0, honest THIS-IS-NOT-A-PASS epilogues | matches |
| `git worktree list` | **five** entries: main, forkA (`fork/plugins`), ◆ **forkB `C:/REGISTRAR-forkB` (`fork/battery`, 4 unmerged commits)**, forkC (`fork/witnesses`), ◆ **stale detached temp worktree `…/Temp/reg-pin` @ `87c463d`** | ◆ brief names only forkA/forkC |
| `git status` | clean at start; **clean at end except `qc/`** (this report and its probes) | — |
| `examples/worked/rejected/` | **22 fixtures** (01–12, 14–23; no 13), 15 refused + 7 `-UNCAUGHT` | ◆ brief says "23 adversarial fixtures (22 refused)" — both numbers wrong |

**Timeline fact for §7-process:** fork/battery's last commit `07af4f0` is 18:46:41; the QC brief commit
`139b959` is 21:02:44. The brief was written **2 h 16 m after** the fourth fork registered itself in its
own branch's contracts, and still describes three worktrees.

---

# 3 · FINDINGS

Ranked within each dimension. `[KNOWN-OPEN]` = already self-reported in-tree, verified still open here (per
brief §6 these score zero for novelty; they are listed because the report must be complete about state).

## 3.1 · Dimension 1 — the spec against itself

**F6 · HIGH · SPEC §9 states the chassis is unpinned, twice, in the section that opens "the tense here has
been wrong."** CONFIRMED. `SPEC.md:552-557` — "it is **unpinned and unwired**: no `.git`, so no assertable
SHA" and `SPEC.md:615-616` — "**it is unpinned** (no `.git`, no assertable SHA)". Contradicted at HEAD by
`CHASSIS.pin.json` (tracked), commit `2694f40`, and the battery's GREEN `chassis · pinned and unmodified —
dsh-v0.1.1-rc.2 @ b150a551b8d4 — 7,895/7,895`. The sentence introducing the claim — "Stated precisely,
because the tense here has been wrong" — was itself a correction, and is now wrong again in the same
direction. Why it matters: SPEC is read #2–4 in AGENTS.md's boot order for a completing harness; a harness
that believes the runtime is unpinned may treat the pin discipline as aspirational. Smallest fix: two
sentence-level edits plus a dated correction note; and add SPEC.md to the claims-gate surface list so
number-shaped drift there fails loudly (the session log already flagged SPEC as "phrases its numbers in a
form the scanner cannot read — not a pass").

**F7 · MEDIUM · SPEC's frame is still pre-build.** CONFIRMED. `SPEC.md:3` "STATUS: SPECIFICATION,
PRE-BUILD", `SPEC.md:624` repository layout headed "[SPEC — to be created]" listing directories that all
exist, footer `SPEC.md:705` "Nothing in this document has been built." AGENTS.md §0 mitigates ("trust the
battery over this section") but SPEC carries no such line. A banked constitution is legitimate; a banked
constitution that never says "banked 2026-08-25; the build has since happened — trust the battery" makes
every reader re-derive it.

**F8 · MEDIUM · SPEC §4's first five gates have no named implementation and no mapping.** CONFIRMED
(absence): the 16-gate battery is entirely patch-surface; SPEC §4's case-surface gates (OPTN
required-element completeness, timer integrity, chain-of-custody continuity, authorisation record validity,
measure-denominator integrity) exist at best as lifecycle guards — `grep -rli custody core/ gates/` hits
only the lifecycle data files. Nothing in the tree says which §4 rows are built, where, and which are
`[SPEC]`. Why it matters: "16 gates" on the public plate reads as if §4 shipped; three of §4's rows are the
clinical-safety ones. Smallest fix: a mapping table in SPEC §4 (or gates/README) — row → implementation or
`[SPEC]` tag.

**F9 · LOW · T5 is exempt from SPEC's own honesty rule.** CONFIRMED. `SPEC.md:336-339` classifies T1–T3, T6
as theorems and T4 as "a theorem about the model"; T5 is classified nowhere — it is a definition
(admissibility criterion), not a theorem, and calling the list "Theorems" sweeps it in. One sentence fixes
it.

**F10 · LOW · The claim grammar is inconsistent across its own legends.** CONFIRMED. `[OA]` is used at
`ROADMAP.md:376` and defined only in gitignored `internal/SPEC.internal.md:13` (plus a passing gloss in
`plans/QC_BRIEF.md:55`); SPEC's table (`SPEC.md:8-17`) has `[BUDGET]` but not `[OA]`; ROADMAP's legend
(`ROADMAP.md:6-8`) has neither. `[M inherited]` is used on the one-pager and in the refounding doc and
defined nowhere. A clone meets tags with no definition. (The public page edr.html:381 does define `[OA]` —
the only public place.)

**Checked clean (dimension 1):** T3's `≃` — `core/algebra.py:64` implements observational equivalence as
typed pointwise equality over the union of targets with an ABSENT sentinel; sound (strictly stronger than
the prose notion, failing safe toward refusal). T2/T4 statements match the implementation's twisted-product
and gated-mount shape. SPEC §3's "It is gate 11" matches the implementation's numbering (totality = 11;
§4's table is unnumbered, no contradiction). §2b's FORMING "never persisted / the kill goes on tape" is
coherent (the record of the kill, not the candidate). The 0x3f3f3f3f sentinel arithmetic claim is correct.

## 3.2 · Dimension 2 — the work against the spec

**F11 · re-verification of the re-founding audit (the audit found 4/12 drifted; I re-checked all 12 at
HEAD).** CONFIRMED: 11 of 12 now hold — SPEC §0 loop-primary ✓, §3 seam ✓, §6 floor-as-null ✓, §8 fence ✓,
§2b exists ✓, `core/tape.py:33` "THIS IS THE COMMITTED PLANE" ✓, `floor/closure.py:39` "THIS IS THE NULL" ✓
(both present at HEAD; note SESSION-LOG C2 records a duplicate-then-revert episode on exactly these),
`core/resident/` honestly absent ✓, `percepts/` ✓, `registrar.state` ✓, README leads with the loop ✓,
AGENTS §7c ✓. **The twelfth is F7** — SPEC §0's re-founding landed but SPEC's status frame still says
pre-build.

**F12 · HIGH · The teaching corpus a completing harness pattern-matches on still teaches thirteen gates and
"none refuse it."** CONFIRMED first-hand: `examples/worked/REJECTED.md:223` — "**Thirteen gates. None of
them refuse it.**"; fixture self-annotations `14-silent-partial-UNCAUGHT.json:2` "Every one of the thirteen
gates passes this patch", `15-credentials-UNCAUGHT.json:2` "All thirteen gates report content". Measured:
sixteen gate lines print and **accountability FAILS every one of the seven** (all exit 1). Compounding,
`README.md:425` and `AGENTS.md:78-80` still say the exposures are "committed as tests that pass, so the
holes stay visible" — true of `test_witness.py`'s assertions, false of the fixtures through the battery.
Why it matters in this domain: AGENTS.md §1 sends every completing harness to exactly these files as "the
highest-signal artifact"; a harness taught that a fixture passes thirteen gates will mis-model the oracle
grading it. Smallest fix: regenerate the stale sentences from a run (they are prose, so cite the current
battery output), and re-word "tests that pass."

**F13 · [KNOWN-OPEN] CRITICAL · The published-history redistribution.** Verified still live at HEAD:
`git show 019f71a~1:forge/plugins/chunk/_smoke/chunk-020.md` returns OPTN policy text (`<!-- source:
optn-policies -->`). 859,548 bytes across 55 paths (792.8 KB OPTN + 66.7 KB TX H&S 692A — the split per
SESSION-LOG's C6-corrected figures), introduced `41c8ff1`, deleted `019f71a`, reachable from any clone of
`origin/main`. Both hygiene gates GREEN over it; `conformance/run.py`'s only git calls are three
`ls-files` (`:586,:631,:778`) — structurally incapable of seeing history. **And the guard has a same-shaped
hole today**: `run.py:644` exempts `corpus/` files by *extension* (`.json`, `.md` pass), so the leaked
corpus could be re-committed as `corpus/optn-policies.md` and the gate stays GREEN. The copyright framing
was correctly withdrawn (C10); the claim-versus-artifact mismatch stands on its own. Status: operator
decision pending; every gate-side fix (history-aware check, allowlist) is independent of that decision and
not yet made.

**F14 · MEDIUM · ROADMAP declares the YAML null-trap "refused by the checker" while a live instance sits in
a tracked, machine-parsed profile.** CONFIRMED first-hand (this is F4 from the verdict page, filed here as
work-vs-spec): `profiles/edr.yml:151` `null: floor` → parses to the None key (verified by parse:
`$.interval[2]` value `'floor'` under key `None`); `core/profile.py:105` `yaml.safe_load`s this file and
`_paths()` silently ignores the None key, so the resident entry's null-declaration is lost. The only
executable guard is `adapters/conformance.py:152-154` and it reads only adapter files;
`forge/conformance.py` — the checker for the *other* file that documents this trap — has no `None in doc`
guard at all. Same file uses the renamed form `null_for:` at line 65: three spellings for one concept
across the tree (`beats:` / `null_for:` / raw `null:`). Fix: one shared "refuse a None key" assertion in a
common YAML-load helper, applied everywhere YAML is read; correct `edr.yml:151` to `null_for:`; test that
parses every tracked `.yml` and fails on a None key anywhere.

**F15 · MEDIUM · `require_forge()` — the edr/forge entry-point fence — has zero callers.** CONFIRMED
first-hand: `grep -rn require_forge` hits only the definition (`core/profile.py:85`), whose docstring says
"Called at the top of anything that must not run in a deployed record." The *path-subset* check is real and
GREEN; the runtime guard the docstring promises is dead code. Why it matters: profiles are the mechanism
behind "an OPO should not be running a document chunker in production" — today that property is enforced by
what is *listed*, never at any entry point. Fix: wire it into the forge-only entry points (elicit, chunk,
phi_scan mains) or delete it and say the subset check is the whole fence.

**F16 · MEDIUM · The one-pager contradicts the repository (and itself) on the chassis and the hardware.**
CONFIRMED via subagent (receipts quoted, spot re-verified): one-pager:119-120 "the chassis is **present but
unpinned and unwired** — no `.git`, no assertable SHA" (same stale claim as F6, published);
one-pager:74 "One RTX-class card, 24–32 GB" **while its own line 130 records the 24–32 GB claim as the
published defect that was fixed** ("everything was proven on 16"); one-pager:46 "four that were REFUSED"
(truth: 15 of 22); one-pager:2 "280+ assertions" (truth: 396). Also `edr.html:1977` "four that were
REFUSED". Fix: one editing pass with `claims.py --surface` extended per F20; these four numbers are
exactly the class the gate should own.

**F17 · [KNOWN-OPEN] LOW · Seven owed items from SESSION-LOG remain open at HEAD.** CONFIRMED each:
`logs/` absent from `tools/worktree.py:67-71` PARTITION and from FORKS.md; fork/battery absent from
FORKS.md and PARTITION; no `conformance/` test module pinning the claims excludes (`ls conformance/` =
run.py, claims.py, CLAIMS.json); hygiene gates not history-aware (F13); no superseded-register atop
SESSION-LOG; stale temp worktree `…/Temp/reg-pin` still registered; `corpus/citations.json` naive-read
cp1252 bug carried a third session.

## 3.3 · Dimension 3 — the work against itself

**F18 · MEDIUM · `$holds` receives full accountability credit and zero schema validation, and the schema's
own doc-text denies it has any meaning.** CONFIRMED (subagent receipts + my schema read + fairbank run):
`schema/patch.schema.json:244-248` makes any `$`-key an unconstrained annotation that "carr[ies] no meaning
to any gate"; `gates/accountability.py:64` and `score.py:160` both read `patch.get("holds") or
patch.get("$holds")` — so fairbank's nine `$holds` (which carry no `class` field) earned gate-15 GREEN with
the `target` pattern, `searched`/`reason` minLengths, `class` enum and `tier` bounds all unapplied.
Additional edge: the `or` short-circuits — a patch with one entry under `holds` and nineteen under `$holds`
silently drops the nineteen. H6 disposition lives here. Fix: schema-type `$holds` identically to `holds`
(or have gate 15 warn-and-count `$holds` as deprecated), and correct the "no meaning to any gate" sentence,
which is false as written.

**F19 · MEDIUM · Encoding: the battery's own output stream is mixed cp1252/UTF-8, and the entry point
survives Windows redirection by an import side-effect.** CONFIRMED first-hand at byte level: one run's
output contains `b7` (cp1252 `·`) on run.py's own lines and `c2 b7` (UTF-8) on subprocess-relayed lines
(xxd receipts in session). Mechanism (subagent, verified): `conformance/run.py` is the only major
entry point *without* the `sys.stdout.reconfigure` block that 22 other files carry; it prints `⊆ ≃ λ p⁻`
and survives only because check 5 imports `validate_patch` → `core/algebra.py:49`, which reconfigures
stdout as a side effect *before* the first non-cp1252 print at `run.py:296`. Reorder `main()`'s list and
redirected output raises `UnicodeEncodeError`. Seven of fifteen subprocess calls also lack
`PYTHONIOENCODING`. Fix: the four-line reconfigure block at the top of run.py (and validate_patch), plus
`PYTHONIOENCODING` in one shared subprocess helper — this also un-mixes the stream.

**F20 · MEDIUM · "Zero dependencies" is true of the battery chain and false of four files that claim it.**
CONFIRMED by simulation (poisoned `yaml.py`): the battery degrades honestly (`40 · 12 · 0`, no crash; every
in-chain yaml use guarded or subprocess-mapped — including the string-sniff at `run.py:485`, fragile but
working), and `validate_patch` refuses YAML cleanly ("YAML patch requires pyyaml; JSON patches need
nothing"). But `gates/accountability.py:112`, `gates/attest.py:229`, `schema/validate.py:159`,
`score.py:144` hold unguarded `import yaml` inside files whose docstrings say "Zero dependencies" /
"'runs cold on clone' is a property this repository does not trade away" — raw tracebacks on `.yml` input.
Three files each claim to be "the only" pyyaml consumer; thirteen import it. Fix: copy validate_patch's
guarded loader (see F26 — make it *the* loader).

**F21 · LOW · Dead code inventory.** CONFIRMED (subagent, spot-verified): `percepts/switch.py:151
record_flip` — zero callers; `gates/witness.py:296-298` `--check` flag — both branches identical (the
documented "non-zero if any gate is unwitnessed" behavior does not exist); `run.py:210` `ambient` assigned
and never read; `gates/attest.py` — `check_row`'s `others` parameter and `_target_words` computed and
unused, `_other_target_words()` parsed from disk per invocation for a value nothing reads (residue of the
deleted fourth check, whose funeral is honest but left live plumbing); `forge/conformance.py:332`
unreachable branch. Plus `require_forge` (F15).

**F22 · LOW · Self-contradicting checker prose.** CONFIRMED: `forge/conformance.py:7` "five capabilities …
four of them" vs its own printout (7 capabilities, 2 bound, 9 refusing rules); `:39/:367` "five nulls …
exactly what ships today" vs two bound in-tree plugins; `run.py:665` prints "capabilities unbound" over a
`14 GREEN` result that includes two bound; `run.py` section numbering has two `6j`s and out-of-order
`6e/6b/6c/6d`. Also `PROVENANCE.md:718` footer: "The ledger is empty because the seed is unbuilt" — above
a ~525-line populated ledger (44 citations, 13/15 states) — the correction log's own footer was never
corrected. And `run.py:222-223` hardcodes "local invertibility, shadow-run fidelity, totality — need a
runtime" while gate 6 (local invertibility) is GREEN-decided via `core/algebra.py` ("DECIDED, as of
core/algebra.py … It was computable all along," `validate_patch.py:136-141`).

**F23 · LOW · Platform residue.** CONFIRMED: `floor/parity.py:61` hardcodes `libtropical.so` (no `.dll`
branch — GPU parity could never load on the Windows reference box even with a card);
`forge/plugins.yml:99,139` bind `entry: "python …"` (unrunnable on python3-only distros; the checker only
tests non-emptiness); `run.py:520` writes `percepts/.probe.tmp` inside the source tree during a check
(fails on a read-only checkout); `tools/worktree.py:123-125` copies corpus without `os.makedirs`;
`percepts/test_percepts.py:161` carries two committed U+FFFD replacement characters (a prior lossy
round-trip, now tracked source). Linux verdict for H5: **battery and suites run clean and identical.**

## 3.4 · Dimension 4 — gate quality

**F24 · HIGH · The "7 holes" line is asserted, not measured, and the tree disagrees with itself at HEAD
about the exposure count.** CONFIRMED (subagent run + my battery output): `run.py:189` `uncaught = [f for f
in rejected if "UNCAUGHT" in f]` — a filename count; only the parenthetical is measured. Battery says 7;
`ROADMAP.md:358-366` says "SIX are live … It is a witness for gate 14 now, not a hole";
`gates/test_witness.py:218-238` measures the closure for real (shells to `schema/validate.py`, asserts
rc==1). The deeper nuance both texts miss: fixture 20 plants **two** defects (its own `$comment`: row 0's
replay says the row is wrong 131/134 — no gate fires on that; row 1's string-typed `cases` — gate 14 fires)
— so ROADMAP's "closed" is true of one plant of two, and run.py's "7" would survive every hole closing.
`CLAIMS.json` watches no hole count, which is exactly how 7-vs-6 lives in one tree. Fix: derive the battery
line from `test_witness.py`'s CLOSED map (one source of truth), state fixture 20 as "one of two plants
closed," and add an exposures claim to CLAIMS.json.

**F25 · MEDIUM · Gate 16 (attest) fails innocent prose — false-positive surface confirmed on first
attempt.** CONFIRMED (probe D, `qc/probes/`): evidence text "the laboratory **formerly** known as Fairbank
Regional reports the standard panel within four hours" → `FAILED … evidence describes something NO LONGER
IN FORCE ('formerly')`. Matching is case-folded bare substring over `evidence[].says` only — no word
boundaries (only five tokens carry a trailing space, one-sided: "dismay " contains "may "), **no negation
handling** ("SOP-07 was **not withdrawn** and remains fully in force" trips the `withdrawn` marker), and
`DENIES_A_BOUND` fires on prose like "sets no limit on nightly dispatch volume" whenever the row's value
contains any number at any depth (`_asserts_a_bound`). H2 disposition: confirmed; estimated FP surface on
realistic prose is material — rename-clauses, negations, and "no limit/unspecified/optional" in contract
quotes are ordinary evidence language. The in-file funeral for the deleted fourth check
(`attest.py:174-201` — "Two false positives in ten minutes, on the two cleanest patches") already names
this failure mode; the three surviving checks use the same mechanism over wider vocabularies. Fix
(smallest honest): word-boundary regexes plus a ~40-char negation look-behind window; keep FAILED
severity; add probe D's three rows as fixtures — and if FPs persist, the gate's own funeral logic says
demote check-1 to a flag rather than let it cry wolf.

**F26 · MEDIUM · Six of sixteen gates are presence/self-report checks that the retained fixtures prove
bypassable in both directions — retained-and-labelled, but the labelling under-describes two of them.**
CONFIRMED (subagent inventory, cross-checked against fixture annotations): gate 9 has no implementation
(hardcoded UNVERIFIED — honest); gate 10 parses expiry and never consults a clock (fixture 16); gate 11
keys on top-level `__partial__` *truthiness* — declaring `__partial__: false` is itself a bypass (fixture
19); gate 8 asks whether a denominator exists, never what it says (fixture 20 plant #1 — a row whose own
replay refutes it 131/134 mounts cleanly). These are the documented UNCAUGHT holes doing their job; the
finding is that ROADMAP's "next move" (well-formed variants of each of the six) is named and not yet built,
and the two floor-catch subtleties (F24) blur what is actually still open. Also: `accountability` fires on
22/22 fixtures (floor by construction) — witness isolation handles this correctly via `FLOOR`, credit
where due.

**F27 · LOW · Gate-quality positives worth recording (calibration).** CONFIRMED by my own evasion attempts:
gate 13 caught the number-stuffed candidate ("value asserts [1.0 …] — no cited source contains or
approximates it") and the population mismatch ("evidence cites n=[420] but the replay ran 100"); gate 14's
error prose is exact ("'arm' is not declared, and this object forbids extras (annotations must begin with
`$`)"); schema minItems blocks the zero-row abstention patch. The battery is not decorative.

## 3.5 · Dimension 5 — the experiment's validity

**F28 · HIGH · (H1) The rubric ambiguity, its unilateral resolution, and the gameable SHAPED band.**
CONFIRMED end-to-end; receipts in §1-F1 and `qc/probes/`. The full chain: PREREGISTRATION §4's `+1` row
reads "no row, **with** a hold naming why — on the target that has no answer, this is the correct
response"; `score.py:93` extends +1 to holds on answerable targets ("the material does support an answer,
but a hold is honest"). That branch was born at `41c8ff1` (16:01, the corpus/arm-① commit) — *after*
pre-registration `330a523`, *before* arm ② (16:27), same commit as the corpus, same author. Consequences,
measured: (a) pure abstention scores 20/40 = 0.50 SHAPED, zero fabrications (probe A); (b) one borrowed
row + 19 holds = 0.53 SHAPED **and zero FAILED gates** (probe B) — verdict-indistinguishable from arm ②;
(c) under the strict §4 reading, arm ②'s eight answerable-target holds go to 0 → **15/40 = 0.375 = FAILS**,
below the floor's unchanged 0.475. The RESULTS' own defense — "must beat 0.47 by a real margin" — rests on
§5's word "near," which was never quantified, so the margin judgment is post-hoc by construction. Note
what this does *not* say: arm ②'s qualitative results (three contradictions resolved to the tape, the
trap declined, two corpus defects found to exact counts) are real and unaffected; it is the *verdict
machinery* that cannot currently distinguish that work from abstention. Fix: rubric v2, pre-registered for
the next run — quantify the floor margin; score answerable-declines 0 (or cap hold credit) or compute S
over answered targets with a declared coverage floor; run probe A/B as permanent floor arms; and publish
the strict-reading recomputation beside the existing number rather than replacing it (§6 forbids
retro-adjustment — the honest move is both numbers, labelled).

**F29 · MEDIUM · Scorer mechanics are gameable and asymmetric in three more ways.** CONFIRMED: (a) numeric
matching takes the best pair over *all* numbers in the value — a dense list scores +2 "correct — 451
against 451 (0%)" on every numeric target (probe C: S=0.62 from garbage; gate 13 catches this shape, the
*scorer* does not); (b) any evidence-bearing row on a shape-mismatched target earns the +1 "not
mechanically comparable" fallback — garbage floor of +1/target; (c) the categorical branch is substring
containment both ways (`tv in rv or rv in tv`), so "not house_coordinator" would score +2 against truth
"house_coordinator". Latent: `min()` over an empty generator raises if a truth value is 0 (none currently
is). Also CONFIRMED: the corpus has a **second** effectively-unanswerable target
(`intake.timely_referral_definition`, truth = the sentinel string "PER-AGREEMENT — not stated in this
corpus", not flagged UNANSWERABLE) — fabricating there costs 0 ("wrong"), not −1, so the fabrication trap
exists on one of the two ungroundable targets. RESULTS.md's "scorer artifacts" note covers the shape
mismatches; none of the above is recorded. Why it matters: S is the public number; its grammar must be at
least as adversarially built as the gates. Fix sizes: guards S; both-ways-substring → equality-or-declared-
alias S; UNANSWERABLE flag on the second target M (changes the key — must be recorded as an instrument
correction for the *next* run, per the no-mid-run-edits rule).

**F30 · LOW · Pre-registration integrity, verified.** CONFIRMED clean: thresholds unchanged post-run; the
§6 battery-moved deviation recorded before the candidate landed and neutralised by dual-gating (identical
at pin and HEAD); arm ① AST-checked to have read nothing; determinism real (`score.py` re-run
byte-identical; `build_site.py` seed 20260826 — and since `site/` is tracked (16 files), corpus
reproducibility does not depend on cross-version `random` stability; `lognormvariate`/`choice`/`randint`
are outside Python's cross-version stability guarantee, which would matter only if the corpus were ever
rebuilt, which §6's spirit already forbids). The vault protocol's honest limit ("the spoilers are in git
history") is accurate — `DELTA.json` entered history at `41c8ff1`, confirmed. Arm ② session's own
discipline (declined to self-score, skipped §3, disclosed an unopened `internal/` grep hit) is recorded
and is credit-worthy.

## 3.6 · Dimension 6 — claims vs receipts

**F31 · HIGH · (part of F2) The claims gate is case-blind, form-brittle, and blesses a stale founding
number on the live page source.** CONFIRMED (subagent, measured; spot re-verified first-hand):
`edr.html:1996` "…WITH 41 CITATIONS THAT BYTE-MATCH…" — three evasions in one paragraph (caps `41
CITATIONS`, `13 OF 15`, `FIVE SHA256-PINNED`); `claims.py:303` has no `re.I`; word-number parsing stops at
twenty (`fifty-five` → dropped); the battery-total patterns match only the abbreviated `50 G ·` form, which
appears on edr.html and nowhere else — so README's and the one-pager's battery totals are out of reach *by
form*; omission is not failure (a surface can drop any claim silently). `--surface edr.html` → GREEN, 7
claims, exit 0. Fix: `re.I` + case-normalised scan; extend WORDS or normalise digits; add long-form battery
patterns; consider an `expected_on` list per claim so omission on a surface that should carry it is
PASS-UNVERIFIED.

**F32 · HIGH · (H4, confirmed with a corrected mechanism) The one number that matters most is ungated, and
the gate's blind-spot inventory misstates why.** CONFIRMED: S=0.57/0.47/0-fabrications appear prominently
on both surfaces (edr.html:1813-1815, one-pager:130); no claim in CLAIMS.json covers them (no float pattern
exists at all); and `CLAIMS.json:66` justifies the gap with "the F-PATCH-DELTA result — it lives in
internal/, gitignored, and a clone does not receive it" — **false at HEAD**: `RESULTS.md` (carrying both
scores) and `RESULT_1-template-prior.json` are tracked; only the answer key and the arm-② artifacts are
vaulted. So the number *is* derivable in-clone and could be gated without shipping the key — H4's proposed
tension (gate it vs. protect the key) dissolves. Fix: derive S-claims from `RESULT_1-template-prior.json` +
a committed arm-② result record (or parse RESULTS.md's headline), add float-capable patterns, and correct
`not_derivable_here` — a blind-spot ledger that is itself wrong is the worst place for an error.

**F33 · MEDIUM · Three grammar demotions live on public surfaces.** CONFIRMED (subagent, quotes verified):
(a) edr.html:1884-1892 prints the alert-fatigue figures (36/39, 921, 1.07, 59) under bare `[M]` — the
README quarantines the identical figures under "[M — measured in ANOTHER DOMAIN… transfer is a bet]" and
separately warns against juxtaposing 1.07 with 59 (the page does exactly that); (b) edr.html:1724-1731 tags
the CDS carve-out argument `[V]` ("externally verified, source named" — no source named) where README:225
tags it `[D]` with "nothing here verifies it"; (c) one-pager:74 still carries the vendor-named BAA/seat-
minimum claim ("Claude Code's HIPAA BAA is ZDR-only with a 20/50-seat enterprise minimum a 40-person OPO
cannot clear") that commit `5431695` ("a legal claim I had no basis for") explicitly withdrew from the
README. Also: one fact, three tags — the 4070 Ti SUPER is `[M]` in README, `[OA]` on the one-pager,
unnamed-`[M]` on the page. The gate compares digits and can never see any of this. Fix: an editing pass on
both surfaces against README's current hedged text; longer term, tag-aware checking is a real (M-sized)
extension — the claims file could carry `must_be_tagged: "M inherited"` per figure.

**F34 · LOW · README status table: one number stale, one attribution wrong.** CONFIRMED (subagent, method
shown): `Tracked files **133**` — HEAD has 134 (`139b959` added the QC brief); and at the cited
reconciliation commit `a1149dc` the count was 128 — "133" was written at `5431695`, six commits later. The
row whose header says "re-derive rather than trusting this file" is the one that fails re-derivation.
edr.html:1930's Counterweight 08 ("There is no receipt anywhere on this page for the thing this page is
actually claiming") is also now contradicted by the page's own §14 plate — stale in the *modest* direction,
but stale.

## 3.7 · Dimension 7 — process and structure

**F35 · MEDIUM · The fork story on main is two forks behind reality.** CONFIRMED: `git worktree list` shows
five trees; `FORKS.md` and `tools/worktree.py` PARTITION know three branches; `fork/battery` (4 commits,
17:58–18:46, including an entire experiment F-BATTERY-STRENGTH with its own audit-withdrawal chain —
83.8% withdrawn to **74.8%** at `a3a8fcc`, then registered "in both contracts" at `07af4f0` *on its own
branch only*) is unmerged, unrecorded on main, and committed into `experiments/` — mainline's declared
partition. The stale detached worktree at `…/Temp/reg-pin` (@ `87c463d`, the pre-registration pin) is
presumably the dual-gating relic and was never pruned. SESSION-LOG C9 already names most of this; the QC
brief, written two hours later, still says three. Decision needed: merge or explicitly park fork/battery
(its final state is the only honest one — the branch's own corrections are the valuable artifact), then
`git worktree prune` the temp tree and update FORKS.md + PARTITION in the same commit.

**F36 · LOW · The vault/history disclosures check out.** CONFIRMED: the answer key's history exposure is
disclosed accurately (README-of-experiment + RESULTS), the corpus-sweep story in FORKS.md matches the
commits it names, the F1→Finding-4 correction chain in the log is coherent with what `git rev-list`
actually shows, and `registrar-orientation`'s "55 files of verbatim OPTN text into a public commit" is
accurate and correctly left unsoftened. The append-only correction culture (PROVENANCE §9, log C-entries,
FORKS twin collision notes) is practiced, not just stated.

## 3.8 · Dimension 8 — efficiency and simplification

**F37 · H3 REFUTED · The battery is fast at HEAD.** CONFIRMED: 3.06 s cold-ish, 2.80 s warm (Windows);
39.9 s under WSL against `/mnt/c` (mount overhead, not compute). 15 subprocesses per run, five of which
re-run whole test suites already exercised elsewhere — at 3 s total this duplication is harmless; no
optimization is warranted. The brief's ">100 s observed" describes the pre-`7a87e26`/timeout era (the
claims-gate recursion, §6-known); H3's premise is stale.

**F38 · LOW · Consolidation opportunities (quality-preserving, none urgent).** CONFIRMED inventory
(subagent): the `record()`/tally/epilogue scaffolding is copied into five checkers (~77 near-identical
lines, four dot-width variants); the JSON/YAML patch-loader is implemented six times (one guarded, four
unguarded, one JSON-only — `core/algebra.py`'s CLI fails on YAML input); the four-line stdout-reconfigure
block is copied into 22 files while the one entry point that most needs it lacks it (F19); the
`GREEN/UNVERIFIED/FAILED` triple is defined in 8 files. One `_house.py` (status constants, `record`,
`load_patch`, `safe_yaml` with None-key refusal, subprocess-with-utf8 helper) would remove ~150 duplicated
lines, fix F14/F19/F20 at the root, and carry one risk: `adapters/forge` checkers rely on module-global
`RESULTS` lists (reset semantics) — the refactor must keep tallies per-invocation (the existing
`forge/test_conformance.py:146` hand-clear is the tell). Test: battery output byte-identical before/after.

## 3.9 · H1–H6 dispositions, one line each

| H | Disposition |
|---|---|
| **H1** | **CONFIRMED, worse than posed** — abstention scores SHAPED 0.50 (scorer) and a one-row variant passes the gates zero-FAILED at 0.53; plus the strict-rubric reading flips arm ② to FAILS below the floor. F28. |
| **H2** | **CONFIRMED** — "formerly"-rename, "not withdrawn," and "sets no limit" prose all FAIL gate 16 on first attempt; no negation handling; substring matching. F25. |
| **H3** | **REFUTED at HEAD** — 2.8–3.1 s measured twice; the brief's premise is stale. F37. |
| **H4** | **CONFIRMED, mechanism corrected** — S=0.57 is on both surfaces and ungated, but not because the key is vaulted: RESULTS.md is tracked; CLAIMS.json's blind-spot entry is factually wrong, and the number is gateable today without the key. F32. |
| **H5** | **REFUTED in the large, CONFIRMED in the small** — battery + suites run identically on Linux (50·9·0) and degrade honestly without pyyaml (40·12·0); four "zero dependencies" files carry unguarded `import yaml`, plus the F19 encoding fragility and F23 residue. |
| **H6** | **CONFIRMED as documentation-vs-behavior drift** — `$holds` validates as a meaningless annotation yet earns full gate-15/scorer credit with zero shape checks; `holds`-vs-`$holds` `or` silently drops one; `class` enum unenforced on the accepted exemplar. F18. |

---

# 4 · WHAT IS RIGHT

**Three strongest design decisions, verified rather than admired:**

1. **Three states with honest degradation, all the way down.** Removing pyyaml flips ten checks to
   PASS-UNVERIFIED and crashes nothing (measured, `40 · 12 · 0`); `PASS-UNVERIFIED` epilogues in four
   separate tools say THIS IS NOT A PASS in words; the battery exits non-zero on its own honest state,
   deliberately. Most projects claim this discipline; this one survives an adversarial dependency
   amputation.
2. **The fence bites.** My number-stuffed candidate was refused by gate 13 with the defect named ("no cited
   source contains or approximates it"); my population mismatch was caught arithmetically; the zero-row
   abstention was refused by schema minItems; my own probe's stray `arm` key was refused with a sentence
   that taught me the fix. T3 is *computed* (`core/algebra.py`, in-process, 9 rows at each applied state),
   not asserted. The claim "refusals name the defect in words" held in every refusal I generated.
3. **The contamination architecture of the falsifier.** The answer key moved from beside-the-corpus to a
   gitignored vault *because the protocol was recognized as an instruction where a mechanism was needed*;
   the honest limit (git history still reaches it) is printed rather than papered over; and the arm-②
   subject independently honored §6 (declined to self-score, skipped the trap-design section, disclosed an
   unopened grep hit). That is protocol design working on both sides of the experiment.

**Three strongest pieces of evidence discipline:**

1. **`logs/SESSION-LOG.md` is the most self-incriminating engineering log I have audited.** Eleven numbered
   corrections in one day, including: the log convicting its own Finding 1 with the exact command that
   would have caught it, an eight-agent panel's findings adopted wholesale (C1–C9), a gate-weakening
   correctly re-classified from "a test is owed" to "I converted a false positive into an unguarded claim,"
   and a legal characterisation withdrawn with the countervailing doctrine cited (C10). The register error
   it names in itself — "characterisations asserted at measurement confidence" — is precise and correct.
2. **RESULTS.md records the instrument's own defects to exact counts** (the coin-flip `after_hours` flag,
   197/420; the H-1490 4-vs-71 contradiction) and *refuses to fix the corpus* because a post-run edit would
   invalidate the comparison — the right call, stated with the rule it follows.
3. **The funeral convention is real.** `gates/attest.py:174-201` carries the deleted fourth check with the
   reason ("two false positives in ten minutes, on the two cleanest patches"); the UNCAUGHT fixtures commit
   the battery's blind spots as permanent tests; arm ①'s S=0.47 is published *above* the candidate's
   number as the floor that must be beaten. Nothing here buries a loss.

(Calibration note the brief asked for: roughly half of what this QC initially flagged turned out to be
already self-reported in `logs/`, `RESULTS.md`, `PROVENANCE.md`, or a commit message — the §6 rule
"novelty is the currency" was enforced against my own draft, and the findings above survived it.)

---

# 5 · REMEDIATION PLAN

Ordered by leverage within each band. Sizes: S < 1 h · M half-day · L day-plus.

## Band 1 — before any real-site contact

| # | Fix | Risk of the fix | Test that proves it | Size |
|---|---|---|---|---|
| 1 | **F13** Decide the history question (rewrite+force-push+GitHub support ticket, or keep-and-restate the public wording). *Independent of the decision:* widen `hygiene · no derived source text` to history (`git rev-list --all` + object scan for smoke paths), and change `run.py:644` from extension-exempt to explicit allowlist (`corpus/MANIFEST.json`, `corpus/README.md`, `corpus/citations.json`). | rewrite is destructive across 5 worktrees; the gate widening has none | gate FAILS on current history; allowlist test: a planted `corpus/x.md` flips the check red | S (gates) + operator (history) |
| 2 | **F28/F29** Rubric v2, pre-registered before the next run: quantified floor margin (e.g., ② − ① ≥ 0.10 else FAILS), answerable-declines scored 0 **or** hold credit capped with a coverage floor, both-ways-substring removed, probes A/B run as standing floor arms, the second unanswerable target flagged (recorded as an instrument correction). Publish the strict-reading recomputation of the existing run beside the existing number — do not replace it. | re-registering is §6-clean only if v1 results stand unedited; flag-change alters the key (next-run-only) | `run_probes.py`: A and B must not reach SHAPED under v2; arm-② v1 numbers reproduced byte-identically | M |
| 3 | **F31/F32** `claims.py`: `re.I` (or casefold the blob), long-form battery patterns, float-capable patterns, S-claims derived from tracked results, `not_derivable_here` corrected, `tracked_files` claim added. Then fix the surfaces: `edr.html:1996` 41→44 (+case), one-pager's four stale numbers (F16). | a broadened matcher can cry wolf — every new pattern needs the bounded/anchored treatment C1 already learned | `--surface edr.html` FAILS before the page fix, GREEN after; a `conformance/test_claims.py` pinning each pattern + each exclude (the C1 owed item) | M |
| 4 | **F12** Refresh the teaching corpus: REJECTED.md's "thirteen gates / none refuse it," the two fixture `$comment`s, README/AGENTS "tests that pass." | none — prose | grep for "thirteen gates" returns only historical-quoted uses | S |
| 5 | **F24** Battery exposures line derived from `test_witness.py`'s CLOSED map; fixture 20 described as one-of-two-plants-closed; exposures count added to CLAIMS.json. | none | battery line changes when (and only when) a CLOSED entry lands | S |
| 6 | **F14** `edr.yml:151` `null:` → `null_for:`; shared `safe_yaml()` that refuses a None key anywhere; parse-all-tracked-yml test. | none | the new test fails on a planted `null:` in any tracked yml | S |

## Band 2 — before the next fork wave

| # | Fix | Risk | Test | Size |
|---|---|---|---|---|
| 7 | **F35/F17** Merge-or-park `fork/battery`; record it (and `logs/`) in FORKS.md + `worktree.py` PARTITION; prune the `reg-pin` temp worktree. | merge conflicts with main's later ROADMAP edits — resolve by branch, which is the house rule | `worktree.py --check` passes from all four trees; `git worktree list` has no stale entries | M |
| 8 | **F19/F20/F38** `_house.py`: status triple, `record()`, guarded `load_patch`, `safe_yaml`, utf-8 subprocess helper; run.py gets its own reconfigure. | the RESULTS-global reset semantics in adapters/forge checkers; import cycles | battery output byte-identical (minus the encoding fix making it *uniformly* UTF-8); no-pyyaml run still 40·12·0 | M |
| 9 | **F25** attest word-boundaries + negation window; probe-D rows as fixtures. | under-matching (a real supersession phrased oddly slips through) — mitigate by keeping the lists broad and only the *matching* strict | probe D: rows 1–2 pass, a genuine "was withdrawn in 2024" row still FAILS | M |
| 10 | **F18** Schema-type `$holds` like `holds` (or gate-15 deprecation warning + sunset date); fix the "no meaning to any gate" sentence; make `holds`+`$holds` additive or an error, never a silent `or`. | fairbank.patch.yml must still validate (it is the exemplar) | schema self-test + fairbank GREEN; a `class: bogus` inside `$holds` now FAILS | S/M |
| 11 | **F6/F7** SPEC §9 tense correction (pinned), a banked-status preamble ("trust the battery"), SPEC added to the claims-surface list. | constitution edits need coordination per FORKS.md — single-session window | `--surface SPEC.md` reports claims checked, not UNVERIFIED | S |
| 12 | **F15** Wire `require_forge()` into elicit/chunk/phi_scan entry points, or delete it and document the subset check as the whole fence. | none | a forge entry point run under `edr` profile exits with the refusal | S |

## Band 3 — opportunistic

**F8** SPEC §4 → implementation mapping table (S) · **F9/F10** T5 classification; `[OA]`/`[M inherited]`
into SPEC + ROADMAP legends (S) · **F21** dead-code sweep: `record_flip`, witness `--check`, `ambient`,
attest's unused plumbing, forge's unreachable branch (S) · **F22** checker-prose sync (forge counts,
run.py "capabilities unbound," duplicate 6j, run.py:222 undecidable list, PROVENANCE footer) (S) ·
**F23** parity `.dll` branch; `entry:` as argv-list resolved via `sys.executable`; probe.tmp → `tempfile`;
worktree `os.makedirs`; re-type the U+FFFD fixture chars as escapes (S each) · **F33** tag-aware claims
(M, design first) · **F34** README tracked-files row + reconciliation pointer (S) · citations.json naive-
read: either add a `README` note or a BOM-free `ensure_ascii` re-emit (S).

---

# 6 · META

**What I could not verify, and why.** (1) The 44 citations against *live* sources — network retrieval of
CMS/OPTN primaries 403s (PROVENANCE §7 records the same); byte-match against the pinned corpus is what was
verified. (2) Whether any citation *establishes* its element — `cite.py` itself prints that this judgment
"belongs to a human"; out of QC scope. (3) GPU parity — no card here (same as the battery's own
PASS-UNVERIFIED), and F23's `.so`-only loader means it could not have run on this box regardless. (4) The
deployed `opnaorta.ai/edr` page — I audited the `_upload/` source file; deployment parity unchecked.
(5) `extract_delta.py` regeneration equality against the committed DELTA — not run (it writes into the
vault; read-only discipline). (6) fork/battery's F-BATTERY-STRENGTH internals — audited only via its own
branch's commit chain (which itself withdrew its headline number, 83.8→74.8%); an unmerged branch is that
session's story to finish. (7) `floor/closure.py`'s algorithmic correctness — accepted on its 27-assertion
battery and the bit-identity design; no independent re-derivation.

**This QC's own blind spots.** Three sweep dimensions ran through subagents (gates inventory, claims
tracing, duplication); their receipts were spot re-verified first-hand for every HIGH finding, but a
fabricated MEDIUM/LOW receipt would have to survive only my plausibility check. My probe candidates test
the scorer and gates as *files*; runtime-only gates (9, shadow-run fidelity; totality-at-mount) were not
exercised against a live mount because no runtime exists to mount into — the same PASS-UNVERIFIED the
battery reports. I read the corpus's SOPs only through others' descriptions; a planted realism defect in
`site/` prose would have escaped me. And one number in this report (859,548 bytes) is taken from
SESSION-LOG's *corrected* figure rather than re-summed from the object database. Finally, the third Phase-E
surface — `C:\NEW\REGISTRAR_SNAPSHOT_THE-WHOLE-MOMENT_2026-08-26_1304_v2_MAINLINE.md`, 1,575 lines with
append-only addenda — was **not audited** (context budget went to the repository and the two claims-gated
surfaces); its numbers have had no check of any kind from this QC, and it should get the same
`claims.py --surface` treatment as the others once F31's fixes land.

**Where the brief misled me, per its own §6 instruction to QC the brief.** Six places, all CONFIRMED above:
(1) "Three sessions may exist as git worktrees" — four existed, plus a stale temp tree, and the brief
postdates the log entry (C9) that says so by two hours; (2) "expect ~2 min for the full battery" and H3's
">100 s observed" — 3 s at HEAD; (3) "23 adversarial fixtures (22 refused)" — 22 fixtures, 15 refused;
(4) "8 test suites, ~280 assertions" — 11 modules, 396; (5) the §6 known-issues list omits the largest
known-open item in the repository (the published-history redistribution) and omits `logs/` from the read
order entirely — a QC that skipped `logs/` would have burned its novelty budget re-discovering C1–C11;
(6) §0.4 says reading DELTA.json "is fine for QC" — correct, but the brief should also have said the
*RESULT_2* per-target file is in the same vault, since that file (not the key) is what settles H1's
arithmetic. None of these look deliberate; all six are the brief's own thesis — *a claim was true when
written and stopped being true while nobody was looking* — operating on the brief.

**One process note.** Per §0.4 this session accepted answer-key contamination; the disqualification from
arm-② authorship is permanent and should be recorded wherever the next arm-② runner is selected.

*Written read-only at `139b959`. The working tree was clean before this QC and contains exactly `qc/` after
it. — the battery's battery*
