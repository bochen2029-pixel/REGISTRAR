# QC REPORT · second full-system quality check of REGISTRAR

**2026-08-27 · audit pinned at `38f9b68` (HEAD moved from `f8c6923` to `38f9b68` mid-audit; both are
recorded) · a zero-context session · read-only except `qc/`.** Scope: everything that changed since the
first QC audited `139b959` — which no longer exists, because the history was rewritten. Its successor is
`69cc588`, and `git log 69cc588..HEAD` (30 commits) is this report's map.

Every claim carries `CONFIRMED` (reproduced here, command and output in the text) or `PLAUSIBLE`
(reasoned; what would confirm it is named). Two sweeps ran through read-only subagents (the history
rewrite; the two PROPOSED documents); **every load-bearing claim from both was re-verified first-hand and
the receipt below is mine, not theirs.**

**Contamination disclosure, accepted per brief §0.4: this session read `internal/f-patch-delta/DELTA.json`,
`DELTA_v1.json`, `DELTA_v4.json`, `build_site_v4.py` and all four `RESULT_*.json` files in the vault, and
is permanently disqualified from authoring any arm-② candidate on any Fairbank corpus.** It has also read
`RESULTS.md`, `CORPUS_v4_DESIGN.md` and both pre-registrations in full.

**Discipline note:** the scorer writes `RESULT_<arm>.json` into `experiments/F-PATCH-DELTA/`. To honour
§0.1 I ran it through a scratchpad shim that rebinds `score.HERE`, so **no scorer artifact was written into
the repository.** The working tree is unchanged by this QC except `qc/` and one pre-existing `?? nul`.

---

# 1 · VERDICT PAGE

| Surface under QC | Verdict |
|---|---|
| The published git history | **GREEN — the standing CRITICAL of the first QC is genuinely closed.** All 40 rewritten commits are *absent from the object database*, not orphaned; 68/68 map rows verified; both fork branches sit entirely on rewritten history. This is the strongest single piece of work in the delta. |
| The machinery (core, floor, gates, percepts, algebra, boot) | **GREEN** — 396/396 assertions across 11 modules, battery `53 · 9 · 0` in 3.3 s, `require_forge` wired, the YAML None-key class closed by a sweeping battery check, tree clean after every probe |
| The falsifier's instrument (`score.py`, the key, the artifacts) | **FAILED** — the scorer has no binding between candidate, corpus and answer key; at HEAD it silently mis-grades the only protocol-valid arm-② artifact; the matcher scores a stated contradiction as a correct answer; and **not one candidate patch or result file is tracked**, so no published S is reproducible from a clone |
| The falsifier's *arithmetic* | **GREEN** — every published number (0.70, 0.62, 0.55, 0.47, 0.45, 0.38) reproduces exactly, first-hand, against the correct key. The numbers are real. The instrument that produced them is not sound. |
| Corpus v4 | **PASS-UNVERIFIED, treated as failure** — one of the three "builder-asserted" invariants is not asserted; the design doc states an unreachable ceiling; the corpus contains a sentence gate 16 refuses; and `CORPUS_v4_DESIGN.md` is a **tracked** spoiler on no do-not-read list |
| The claims gate and the public surfaces | **FAILED** — `README.md` publishes `51 GREEN` (truth 53) and `133` tracked files (truth 182); the one-pager still carries all four stale claims the first QC named; and the gate is blind to every one of them **by form**, because the long-form patterns its own remediation ordered were never added |
| The two PROPOSED documents | **FAILED as drafts (correctly parked as proposals)** — one asserts `GREEN` over a check the battery prints `PASS-UNVERIFIED`; one would make the constitution contradict the shipped schema; one deletes a doctrine sentence under a changelog line saying nothing was removed |
| The remediation story | **PASS-UNVERIFIED, treated as failure** — commit `7646f98` is titled *"every QC mechanical finding closed"*; at least four Band-1 items are open, and two of them are now producing live wrong numbers |

**The five findings that matter most:**

1. **G1 · The scorer, the corpus and the answer key are not bound to each other, and the documented repair
   reverses the key** (CONFIRMED). `score.py:50` loads `internal/f-patch-delta/DELTA.json`, which is today
   a copy of the **v4** key. Scoring `fairbank.v2.patch.yml` — the *only* artifact `PREREGISTRATION_v3.md`
   says carries a verdict — at HEAD with no flags returns **`16/40 · S = 0.40 · FAILS`** where the
   published number is **`15/40 · S = 0.38`**. No warning, no corpus field, no rubric field in the output.
   The scorer has no `--delta`; `RESULT_<arm>.json` records neither rubric nor floor nor corpus; and all
   four Fairbank candidates carry `arm: None`, so **every one of them writes the same
   `RESULT_unknown.json`.** Compounding: `.gitignore:18 *.patch.yml` means **no candidate and no result
   file has ever been tracked** — a clone receives RESULTS.md's prose and nothing to recompute it from.
   And the experiment README's own repair instruction (`python internal/f-patch-delta/extract_delta.py`)
   writes the **v1** key over `DELTA.json`, silently converting every v4 number into a wrong one.

2. **G2 · Rubric v3's new leaf matcher scores a stated contradiction as a correct answer, and the COVERS
   knife-edge rides on it** (CONFIRMED). `score.py:116` — `if t and cs and (t in cs or cs in t)`. Measured:
   truth `house_coordinator`, candidate value `not house_coordinator` → **`(2, "correct")`**. The first
   QC's remediation Band-1 item 2 required "both-ways-substring removed" in rubric v2. It was never
   removed — and **v3 propagated it into `_leaf_match`, the very change v3 exists to make.** Instrumented
   on the published run: **11 of run A's 28 points come from the leaf branch, and 4 of its 16 matched
   leaves matched by substring containment only, not equality.** COVERS is `S ≥ 0.70` and run A is exactly
   `28/40`; RESULTS.md says "one leaf either way moves the band."

3. **G3 · "Every QC mechanical finding closed" is false, and two of the open ones are live wrong numbers**
   (CONFIRMED). `README.md:391` publishes **`51 GREEN · 9 PASS-UNVERIFIED · 0 FAILED`** in the row whose
   own "how to check" column says `python conformance/run.py` — which prints **53**. `README.md:389`
   publishes **`Tracked files 133`** beside `git ls-files | wc -l` — which prints **182**. The one-pager
   still carries all four claims the first QC confirmed stale (`50 GREEN · 280+ assertions`, *"the chassis
   is present but unpinned and unwired"*, *"four that were REFUSED"*, `24–32 GB` on the same line that
   records 24–32 GB as the fixed defect) **plus the legal claim commit `5431695` withdrew.**
   `REJECTED.md:223` still teaches *"Thirteen gates. None of them refuse it."* The `re.I` half of the
   claims fix landed; **the long-form battery patterns the same remediation item ordered did not**, so the
   gate returns GREEN over every one of these.

4. **G4 · The two PROPOSED documents each contain one amendment that must not be adopted as written**
   (CONFIRMED). `ROADMAP_v2_PROPOSED.md:35` states as a headline receipt *"claims gate holding two public
   surfaces GREEN"* — the battery prints `PASS-UNVERIFIED claims · public surface … no REGISTRAR_SURFACE
   set — nothing to compare`. **GREEN asserted over a printed PASS-UNVERIFIED, in a document whose own §4
   makes that the defect.** `SPEC_v0.2_PROPOSED.md:127` appends `holds` to the table headed *"Every row
   must carry all of the following, or it does not validate"* — but `holds` is patch-level, optional, and
   `$defs/row` sets `additionalProperties: false`, so a row literally carrying it would be **refused by
   gate 14.** And `ROADMAP.md:290`'s resident-emission rule — *"may escalate the shape of a problem, never
   the material"* — exists nowhere else in the tracked non-`internal/` tree and is **absent from v2**,
   under a changelog line reading *"Nothing removed as wrong."*

5. **G5 · Corpus v4 claims a builder guarantee it does not make, and carries a tripwire for the repo's own
   gate 16** (CONFIRMED). `RESULTS.md:468-470` — *"all three construction invariants asserted by the
   builder itself"*. `build_site_v4.py` asserts **I1 and I2 only**; I3 (every volume claim printed from the
   tape — precisely the v1 defect it was introduced to close) has **no assertion**, only a `print`.
   Separately, `site_v4/ops/SOP-03-referral-intake.md:4` reads *"route to the House Coordinator (rev 7
   change; **formerly** the on-call supervisor)"* — the primary source for a planted contradiction. A
   candidate quoting it for the *correct* value is **FAILED by gate 16** ("evidence describes something NO
   LONGER IN FORCE"), exit 1. Both v4 candidates happened never to quote it, so `0 gate-FAILED` held by
   avoidance rather than by soundness. And `CORPUS_v4_DESIGN.md` — which names both designed declines and
   the trap directions — is **tracked** and on no do-not-read list, while the ledger it belongs to stopped
   growing two runners ago.

**The single sentence for the operator:** the history is genuinely clean and every published falsifier
number reproduces exactly — but the instrument that produced them cannot tell you which corpus it graded,
scores "not X" as X, and keeps none of its evidence in the repository; fix the instrument's bookkeeping
before the next number, and fix the four surfaces that are wrong today, because a repository that publishes
`51 GREEN` next to the command that prints `53` has already lost the argument it is making.

---

# 2 · GROUND TRUTH

Derived, per the brief's delta, from `conformance/CLAIMS.json`, the battery and the suites — not from the
brief's §1 block, which is superseded.

| Command | Result |
|---|---|
| `python conformance/run.py` | **`53 GREEN · 9 PASS-UNVERIFIED · 0 FAILED`**, exit 2 (deliberate) |
| — timed | **3.27 s** cold, 3.3 s warm — 16 checks groups, `claims.py --check` now runs the mutation harness inside it |
| all 11 test modules | 23+40+27+42+48+26+30+34+77+39+10 = **396 passed, 0 failed** |
| `tools/cite.py --check` | 44/44 byte-exact |
| `tools/pin_chassis.py --check` | 7,895/7,895 byte-identical, offline merkle |
| `conformance/claims.py --check` | `CLAIMS.json current — 14 claims agree`, exit 0, **3.62 s** |
| `--surface edr.html` | GREEN — 7 claims (**battery totals now reachable: page uses `53 G · 9 PU · 0 F`**) |
| `--surface` one-pager | GREEN — 4 claims (battery totals **out of reach by form**; the page says `50 GREEN`) |
| `--surface README.md` | GREEN — 4 claims (battery totals out of reach; the file says `51 GREEN`) |
| `git ls-files \| wc -l` | **182** (README says 133) |
| `examples/worked/rejected/` | **22** fixtures, **7** `-UNCAUGHT` |
| battery exposures line | `7 hole(s) **MEASURED** open … (run per fixture, not counted by filename)` — the first QC's F5/F24 is genuinely fixed |
| `demo/replay.py --selftest` | `SELFTEST GREEN — two replays, 93 transcript lines, byte-identical` |
| — cross-process, `PYTHONHASHSEED` 1 vs 999 | transcripts identical; **percept streams byte-identical too** (I compared them; the selftest does not) |
| `score.py` v4 run A, `--rubric v3 --floor 0.47` | `28/40 · S = 0.70 · 0 fab · answered 17/19 · **COVERS**` ✓ matches RESULTS.md |
| `score.py` v4 run B | `25/40 · S = 0.62 · 0 fab · answered 16/19 · **SHAPED**` ✓ (true value 0.625) |
| floor vs v4 key, v3 | `19/40 · S = 0.47 · **1 fabrication**` ✓ |
| v2 candidate vs **v1** key, v3 | `15/40 · S = 0.38 · FAILS` ✓ |
| v1 candidate vs v1 key, v3 | `18/40 · S = 0.45 · FAILS` ✓ |
| floor vs v1 key, v3 | `22/40 · S = 0.55 · 1 fab` ✓ |
| **v2 candidate at HEAD, no `--delta`** | **`16/40 · S = 0.40 · FAILS`** ◆ silently graded against the v4 key |
| `git worktree list` | main `38f9b68`, forkA `a59fbf2` (`fork/plugins`), forkC `3dfb23a` (`fork/witnesses`, **concurrent — not audited**), and the **stale detached `…/Temp/reg-pin` @ `87c463d`, still unpruned** |
| `git status` | `?? nul` — a 0-byte file dated 2026-08-26 22:38, the Windows artifact of a `> nul` redirect under a POSIX shell. Untracked, present before and after this QC. |

**Every published falsifier number reproduces exactly.** That is worth saying first, because the findings
below are all about the *instrument*, not the arithmetic.

---

# 3 · FINDINGS

`[KNOWN-OPEN]` = already self-reported in-tree and verified still open (per brief §6 these score zero for
novelty; listed for completeness of state).

## 3.1 · The falsifier arc, end to end

**G1 · CRITICAL · No binding between candidate, corpus, key and rubric — the instrument silently
mis-grades its own archive.** CONFIRMED, four ways.

*(a) The key is a moving file with a fixed name.* `experiments/F-PATCH-DELTA/score.py:49-57` loads
`internal/f-patch-delta/DELTA.json`. On disk: `DELTA.json` and `DELTA_v4.json` are both 4,368 bytes with
the same mtime; `DELTA_v1.json` (6,981 B) is the v1 corpus key. `internal/f-patch-delta/build_site_v4.py:284`
writes `DELTA_v4.json`, **not** `DELTA.json` — so the active key got there by an unrecorded hand copy.
Measured consequence:

```
$ python <shim> experiments/F-PATCH-DELTA/fairbank.v2.patch.yml --rubric v3 --floor 0.55
  score 16 / 40   S = 0.40      verdict (v3): FAILS
$ python <shim> …fairbank.v2.patch.yml --rubric v3 --floor 0.55 --delta internal/f-patch-delta/DELTA_v1.json
  score 15 / 40   S = 0.38      verdict (v3): FAILS
```

`PREREGISTRATION_v3.md:33` — *"Only the **v2-run candidate** (`fairbank.v2.patch.yml`) carries a verdict —
it is the only protocol-valid arm-② artifact."* At HEAD, re-scoring that artifact returns a wrong number
that is plausible enough to be believed. There is no `--delta`, no `--corpus`, and nothing in the output
names which key was used.

*(b) The repair instruction reverses the key.* `experiments/F-PATCH-DELTA/README.md:68-72` tells any
runner: *"Scoring regenerates the answer key from the vault. If it is missing: `python
internal/f-patch-delta/extract_delta.py`."* That extractor reads `experiments/F-PATCH-DELTA/site` (the
**v1** corpus — `extract_delta.py:29`) and writes `HERE/DELTA.json` (`:236`). Following the documented
recovery path silently replaces the v4 key with the v1 key.

*(c) The result artifact is not identified.* `score.py:280-291` emits `arm · candidate · score · max · S ·
fabrications · verdict · per_target` — **no rubric, no floor, no corpus, no key digest** — into
`RESULT_{patch.get('arm','unknown')}.json`. Measured: all four Fairbank candidates carry `arm: None`, so
run A and run B and the first QC's probes all write **`RESULT_unknown.json`**. The vault's four result
files confirm the collision is real, not hypothetical: three of them carry `"arm": null`.

*(d) Nothing is tracked.* `git check-ignore -v` → `.gitignore:18 *.patch.yml` on every candidate;
`git ls-tree -r HEAD experiments/F-PATCH-DELTA/ | grep -i result` → only `RESULTS.md`. **No arm-② candidate
and no result file has ever been tracked in any commit.** The corpus is committed (`site/`, `site_v4/`);
the *evidence for the headline* is not. The ignore rule that causes this exists for an excellent reason
(never commit a real site's patch) and was never revisited for a synthetic one.

*Why it matters here:* S is the number on the public page. A repository whose thesis is "a number without
its grain, denominator and source is not a number" currently publishes six S values whose source cannot be
re-derived by anyone but this machine, from an instrument that will not tell you which corpus it graded.

*Smallest honest fix:* `score.py` takes `--delta`/`--corpus`, refuses to run without one, records
`{rubric, floor, corpus, delta_sha256}` in the result, and names the result
`RESULT_<corpus>_<rubric>_<candidate>.json`; add `!experiments/F-PATCH-DELTA/*.patch.yml` to `.gitignore`
and commit the four candidates and their results (they are synthetic, and the seed's own rule is that the
prohibition protects *real* site material).

**G2 · CRITICAL · The rubric-v3 matcher scores a contradiction as agreement, and the published COVERS band
depends on it.** CONFIRMED first-hand.

`score.py:106-118` (`_leaf_match`, v3's change 2) and `score.py:165-168` (the categorical branch) both
match strings by two-way containment:

```
score.py:116    if t and cs and (t in cs or cs in t):
score.py:166    if tv.lower() in rv.lower() or rv.lower() in tv.lower():
```

Probe, run against the real module:

```
truth   : house_coordinator
filed   : not house_coordinator      -> (2, "correct — 'not house_coordinator'")
filed   : house                      -> (2, "correct — 'house'")
_leaf_match('in_house', ['not in_house'])  -> True
```

The first QC's remediation Band-1 item 2 listed *"both-ways-substring removed"* among the two changes
rubric v2 was to make. `PREREGISTRATION_v2.md` made two changes and this was not one of them; v3 then
carried the same predicate into the new leaf matcher. Exposure, instrumented against the published runs:

| candidate | total | leaf matches: exact | **substring-only** | numeric |
|---|---|---|---|---|
| `fairbank.v4.patch.yml` (run A, COVERS) | 28/40 — 11 pts from the leaf branch | 12 | **4** | 0 |
| `fairbank.v4b.patch.yml` (run B, SHAPED) | 25/40 | 11 | **2** | 0 |

Run A's COVERS is exactly `28/40 = 0.70`, and `RESULTS.md:482` states the margin is zero. **The band
assignment therefore rests on a matcher that cannot distinguish a value from its negation.** No fabrication
claim is affected (fabrication is an evidence test, not a value test), and I found no *actual* inverted
match in either candidate — the finding is that the instrument cannot rule one out.

*Smallest fix:* equality after normalisation, plus an explicit `aliases: []` list per truth leaf; refuse
containment. Re-score all six recorded arms symmetrically and publish the delta as an instrument
correction, not a regrade.

**G3 · HIGH · The COVERS band is the one band the scorer does not check against its own pre-registration.**
CONFIRMED. `PREREGISTRATION_v2.md:51` — *"**COVERS** — S ≥ 0.70, zero fabrications, answered ≥ 8, **gates
pass**"*. `score.py:271-272`:

```python
    elif S >= 0.70 and fabrications == 0:
        verdict = "COVERS"
```

No gate check (the scorer never sees the gates — by design) and, unlike SHAPED, no floor clause. The
published run happens to satisfy both (`0 of 16 gates FAILED`, floor 0.47), so **the number is not wrong —
the instrument is.** This is the exact class RESULTS.md:457-461 congratulates itself for catching in the
SHAPED band ("the F1 class recurring inside the fix for F1"), one band over.

*Fix:* `score.py` takes `--gates <FAILED-count>` (or refuses to print COVERS without it) and prints
`COVERS (PROVISIONAL — gate result not supplied)` otherwise, exactly as it already does for `--floor`.

**G4 · HIGH · Corpus v4 claims three builder-asserted invariants and asserts two.** CONFIRMED.

`RESULTS.md:468-470` — *"design frozen first; builder in the vault; **all three construction invariants
asserted by the builder itself** — funnel reconciliation, clock-derived flags, documents printed *from* the
tape"*. `internal/f-patch-delta/build_site_v4.py:5-9` makes the same claim ("the builder **REFUSES** to
emit a corpus that violates them"). The assertions:

```
build_site_v4.py:146   assert all((r["arrived_hour"] < 7 or …) == bool(r["after_hours"]) …), "I2"
build_site_v4.py:148   assert ca <= cw and cr <= ca and {a["case_id"] for a in au} == ca, "I1 funnel"
build_site_v4.py:149   assert len(cw) == len(prog_ids), "I1 workup=progressed"
build_site_v4.py:151   print(f"  invariants I1 I2 hold; H-1490 referrals = {n1490} …")
```

**I3 has no assertion.** It is satisfied by construction for one interpolated number and by nothing else.
I verified the corpus is in fact compliant — `referrals.csv` has 34 H-1490 rows against
`inventory.md:8` "low volume — 34 referrals on this extract" ✓, and H-1102 at 182 is genuinely "highest
volume" ✓ — so **the corpus is sound and the claim about it is not.** The specific failure I3 was
introduced to prevent (v1's *"four referrals"* vs a tape of 71) is closed by a printing convention, and
any *other* volume claim added later is unguarded.

*Also:* `CORPUS_v4_DESIGN.md:37` — *"Honest ceiling under v4 = 40/40, so the bands finally mean what they
say."* Its own rule 2 keeps `transport.perfusion` unanswerable, and a correct decline is `+1`, so the
ceiling is `19×2 + 1 = 39/40 = 0.975` — which the builder prints (`:285`) and RESULTS.md carries. A
pre-registered design document states a ceiling its own design makes unreachable, and nothing records the
correction.

**G5 · HIGH · The corpus contains a sentence the repository's own gate 16 refuses as evidence.** CONFIRMED.

`experiments/F-PATCH-DELTA/site_v4/ops/SOP-03-referral-intake.md:4`:

> **After-hours referrals route to the House Coordinator** (rev 7 change; formerly the on-call supervisor).

That is the *current, correct* rule and the primary document source for the planted org-chart-vs-practice
contradiction. Quoting it as evidence for the correct value:

```
$ python gates/attest.py <probe with that sentence as evidence[].says>
  intake.after_hours_owner: evidence describes something NO LONGER IN FORCE ('formerly')
    and is offered for a current value. …
FAILED — the evidence does not support the value it is offered for.        exit 1
```

`grep -c formerly` on both v4 candidates → **0 and 0**. So the published `0 of 16 gates FAILED` for both
runs is true, and held **because neither runner quoted the corpus's most direct sentence.** The first QC's
H2/F25 named this false-positive class on 2026-08-26 and prescribed word-boundaries plus a negation window
(Band-2 item 9). `git diff 69cc588 HEAD -- gates/attest.py` shows exactly one change: a `try/except
ImportError` around `import yaml`. The gate is unmodified, and the falsifier's own corpus now contains a
live instance of its blind spot.

**G6 · MEDIUM · The disqualification ledger stopped growing while the spoiler surface tripled, and one
spoiler is tracked.** CONFIRMED.

`experiments/F-PATCH-DELTA/README.md:102` states the rule: *"The spoiler surface **grows**: every analysis
of the run adds to it."* The ledger names the mainline session, the QC session, and (in the `2026-08-27`
addendum, `:106`) the v2 runner `a763d362968919d48`. Since then:

```
$ grep -c a763d362968919d48 …/README.md   -> 1
$ grep -c a17186d3cfeb0fb3c …/README.md   -> 0     (corpus-v4 run A runner, RESULTS.md:471)
$ grep -c a061c6936fc8b2a9d …/README.md   -> 0     (replication run B runner, RESULTS.md:513)
$ grep -n "site_v4\|v3\|CORPUS_v4\|PREREGISTRATION_v" …/README.md   -> (nothing)
```

The experiment's front door still describes `site/` as "the corpus", still says a valid runner works "only
under **rubric v2** once it is pre-registered", and never mentions `site_v4/`, v3, or the v4 design. A
runner who follows it literally builds against the v1 corpus and is then scored against the v4 key (G1).

**And the do-not-read list omits a tracked spoiler.** `README.md:29-37` and `RESULTS.md:157` list
`DELTA.json`, `extract_delta.py`, `build_site.py`, `RESULTS.md`, `PREREGISTRATION.md §3`.
`experiments/F-PATCH-DELTA/CORPUS_v4_DESIGN.md` is **tracked** (`git ls-files` confirms) and its §Design
rules 1–3 name both designed declines by target, the trap directions, and the fact that the contradictions
sit on different targets than v1. `PREREGISTRATION_v2.md:48` likewise publishes calibration numbers (its
own §"Who may run it" bars them, but the file is public). This is the *exact* error the experiment already
diagnosed once — "an instruction where a mechanism was needed" — except the v1 spoilers were vaulted and
this one is committed.

**G7 · [KNOWN-OPEN] MEDIUM · `$holds` still earns full credit with zero schema shape checks, and the `or`
still silently drops a collection.** CONFIRMED, unchanged since the first QC's F18/H6.
`schema/patch.schema.json` declares no `$holds`; `patternProperties: ["^\\$"]` admits it as an
unconstrained annotation. `gates/accountability.py:64` and `score.py:231` both read
`patch.get("holds") or patch.get("$holds") or []` — a patch with one entry under `holds` and nineteen under
`$holds` silently loses the nineteen, in the gate that exists to prove silence was accounted for.

**G8 · LOW · Pre-registration integrity, re-verified and clean.** CONFIRMED. v2 and v3 are both dated
before their first score; v3 names its own honesty problem in its opening paragraph and applies
symmetrically to every arm (verified: I reproduced all six v3 scores); the v1 run stands unedited and
labelled void; the corpus is not edited after a run; `RESULTS.md:511` **withdraws its own COVERS headline
on one replication**, which is the strongest single act of discipline in the delta.

## 3.2 · The history rewrite

**G9 · GREEN, and the strong form of the claim holds.** CONFIRMED (subagent sweep, key steps re-run by me).
The first QC's standing CRITICAL is closed *properly* — the material is gone, not orphaned:

```
$ git show 89d8181~1:forge/plugins/chunk/_smoke/chunk-020.md
fatal: path '…' does not exist in '89d8181~1'
$ git rev-list --all --objects | grep -icE "_smoke"          -> 0
$ awk 'NR>1 && $1!=$2 {print $1}' provenance/…commit-map | while read s; do
      git cat-file -e "$s" 2>/dev/null && echo "PRESENT $s"; done   -> (no output)
```

All 40 remapped pre-rewrite commits are **absent from the object database**; no `refs/original/`, no
`refs/replace`, no grafts, one pack dated the rewrite, largest blob in the ODB is `PROVENANCE.md` at
58,832 B (the 784 KB OPTN blob exists nowhere). All 68 map rows verified: 28 identity + 40 remapped, zero
leaks, every new SHA an ancestor of `main`. Both fork branches contain **no** pre-rewrite commits. The
`87c463d` pin survived as an identity row, which is exactly what `PROVENANCE.md:638-640` claims.

**G10 · MEDIUM · Three defects in the *story* around a clean rewrite.** CONFIRMED.

- `PROVENANCE.md:633` — *"Six paths purged from all **69** commits."* The map has 68 data rows
  (`awk 'NR>1' … | grep -c .` → 68) and `git rev-list --count c363609` → 68. **A commit count was read off
  a file's line count, header included.**
- **The destructive command itself has no artifact.** `grep -rniE "filter-repo --|--invert-paths|--path-glob"`
  over all tracked files returns nothing. The "six paths" figure is prose only, unverifiable from the
  repository. In a project about claim-versus-artifact, the one irreversible act is the one with no receipt.
- **No post-rewrite fresh clone was verified.** The only fresh-clone evidence in the tree
  (`logs/SESSION-LOG.md:319-322`) *predates* the rewrite and confirmed the leak was live. My evidence is
  local-only; a clean clone is highly likely and **unproven**. This one is cheap: `git clone --depth=1` into
  temp, `git rev-list --all --objects | grep _smoke`, paste the output.

**G11 · MEDIUM · 22 distinct SHAs cited in tracked documentation no longer resolve — including in
`README.md` and `AGENTS.md`.** CONFIRMED (subagent method: hex-token extraction + `git cat-file -t`, then
intersected with the map's old column). `logs/SESSION-LOG.md` 12 · `qc/QC_REPORT_2026-08-26.md` 8 ·
`experiments/F-BATTERY-STRENGTH/RESULTS.md` 3 · `FORKS.md` 3 · `README.md` 1 (`a1149dc`) · `AGENTS.md` 1
(`a1149dc`). The map exists precisely to translate these, and `PREREGISTRATION_v2.md:11-12` shows the right
pattern ("translates through `provenance/…commit-map`"), but the two front-door documents cite a SHA a
clone cannot resolve and do not point at the map.

**G12 · [KNOWN-OPEN] MEDIUM · The hygiene gate is still `ls-files`-only and still extension-exempt, and the
one residual the rewrite could never reach is still shipping.** CONFIRMED.
`conformance/run.py:669` is the sole data source for both hygiene checks (`git ls-files`); no `rev-list`,
`cat-file` or `git log` appears anywhere in the file. `:682-683` still exempts `corpus/` by extension, so a
planted `corpus/anything.md` passes. Both fixes were Band-1 item 1 of the first QC and both are open;
`logs/SESSION-LOG.md:565` honestly lists the first as owed. Separately and measured here:
`corpus/citations.json` carries **44 quoted strings, 7,753 characters, longest 649, in a 24,550-byte
tracked file — 31.6% verbatim third-party text**, which no history rewrite can touch because it is at HEAD
by design.

## 3.3 · F-BOOT

**G13 · MEDIUM · The boot's own doctrine contradicts itself about what the pin buys.** CONFIRMED.
`forge/boot.py:17-19` — *"the chassis must verify byte-for-byte … BEFORE anything executes from it.
Installing unverified third-party code is the hazard; **the pin makes it unreachable.**"* Nine lines later,
`:21-23` — *"the PIN **excludes build artifacts (node_modules, dist) from its digests**, so it stays
GREEN."* Both cannot be true. `tools/pin_chassis.py:52` — `SKIP = {"node_modules", ".git", "dist",
".turbo", ".cache"}`. Step 2 runs bare `pnpm install` (`:136`), which fetches the npm dependency closure and
executes its lifecycle scripts — code the pin has never seen. What the pin *actually* buys is real and
worth stating precisely: `pnpm-lock.yaml` and `package.json` are inside the pinned walk, so the dependency
*content* is pinned transitively by the lockfile's integrity hashes — but no `--frozen-lockfile` is passed
and install scripts still run. *Fix:* pass `--frozen-lockfile`, and rewrite §1 to "the pin makes the
chassis's own sources verified before execution; the dependency closure is pinned by the chassis's own
lockfile, which is under the pin; their install scripts execute."

**G14 · MEDIUM · The pin's own "say WHICH files" diagnostic is unreachable.** CONFIRMED — and it only
manifests on failure, which is why nobody has seen it.

```
tools/pin_chassis.py:~121   ref = pin.get("_file_digests")
                            if ref:  # ← never taken
$ python -c "import json;d=json.load(open('CHASSIS.pin.json'));print('_file_digests' in d)"
False
```

`CHASSIS.pin.json` carries only `verification.tree_digest_local` (a merkle), no per-file map. So on a real
tamper the gate degrades to *"the local tree does not match the pin / recorded … / computed …"* — precisely
the outcome the code's own comment calls unacceptable (*"say WHICH files, because 'something changed'
teaches nothing"*). *Fix:* emit `_file_digests` into the pin (7,895 entries, ~600 KB) or recompute the
upstream digests on failure from the tarball.

**G15 · LOW · Three smaller boot defects.** CONFIRMED.
(a) `boot.py:11` advertises `verify → install → **build** → launch` in the usage line, directly above 7
lines explaining that build *"DOES NOT EXIST … Install is composition. Build is a fork."* The first line an
operator reads names the forbidden step.
(b) `boot.py:149-150` prints `GREEN  build forbidden …` **unconditionally** — a statement with no check
behind it, wearing the grammar's strongest tag.
(c) `boot.py:190-204` `--status` returns 0 while printing two `PASS-UNVERIFIED` lines, and runs
`step_pin()`/`step_toolchain()` (which spawn subprocesses) **before** `require_forge` — which `main()`
applies only on the non-`--status` path. `conformance/run.py` exits 2 on PASS-UNVERIFIED deliberately; a
sibling entry point exiting 0 on the same state is a grammar leak into CI.
(d) `boot.py:169-174` accepts *any* `HTTPError` as "up" while the success path requires `status < 500`, so
a 500-ing server reports `GREEN harness answers`.

**G16 · CREDIT · `require_forge` is now wired, and proven both ways.** CONFIRMED — the first QC's F15 is
closed: `forge/boot.py:212`, `forge/plugins/chunk/src/chunk.py:492`, `forge/plugins/phi_scan/src/phi_scan.py:392`,
with the plugin test suites declaring the forge profile rather than the fence being weakened.
**But the fix cites the wrong finding.** `chunk.py:486` and `phi_scan.py:386` both say *"QC F5 found
require_forge() with zero callers"* — that is **F15**; F5 was the exposures-line finding. Commit `7646f98`
makes the same two mis-numberings ("F4: the exposures line" is F5; "F5: require_forge" is F15). In a
repository whose value is traceable receipts, the pointer from fix to finding is broken in source comments
that will outlive the commit.

## 3.4 · The replay board

**G17 · MEDIUM · The determinism gate is narrower than its name, and deletes the artifact it should
compare.** CONFIRMED. `demo/replay.py:430-446` runs `run_once` twice to `selftest_a.jsonl` /
`selftest_b.jsonl`, then:

```python
        for pth in ("selftest_a.jsonl", "selftest_b.jsonl"):
            os.remove(os.path.join(OUT, pth))
        if a == b:
            print(f"\nSELFTEST GREEN — two replays, {len(a)} transcript lines, byte-identical.")
```

`a` and `b` are the **board transcript** (a list of display strings). The two **percept streams** — the
append-only artifact SPEC §7's replay-determinism obligation is actually about — are written, never read,
and deleted. `conformance/run.py:842-848` docstrings this as *"SPEC §7: two independent replays of one tape
must be byte-identical"*, and the battery prints it as `board · replay determinism … byte-identical`.
Also, both replays run **in the same process**, so cross-process nondeterminism is structurally out of
reach. I tested what the gate does not: streams from two separate runs are byte-identical (`cmp` silent),
and transcripts are identical across `PYTHONHASHSEED=1` vs `999`. **The property holds; the check does not
check it.** *Fix:* compare the two stream files too, and run the second replay in a subprocess.

**G18 · MEDIUM · The board prints "every budget above is the site's own tape" under chains that are half
invented.** CONFIRMED, from the run:

```
        + lab p90 282m (n=149; contract says 240)              15:12
        + serology gates the match run · 15m [D]               15:27
        + offer budget p90 236m (n=94)                         19:23
        + scheduling gap 15m [D]                               19:38
        + mobilisation p90 118m (n=50, in-house)               21:36
        + incision to clamp 30m [D]                            22:06
    every budget above is the site's own tape.
```

Three of the six rows are `[D]` constants (`replay.py:185, 189, 192`); in the third box, **two of three**
are. Each row is individually honest and the summary line generalises past all of them. *Fix:* "every
budget marked with an n is the site's own tape; the `[D]` rows are composed."

**G19 · MEDIUM · The marquee states a deadline and then shows a different derivation.** CONFIRMED. The
headline says *"serology_resulted must be in by **16:06**"* — the latest-time (backward) solution — and the
chain printed immediately under *"it is the transitive consequence of:"* is the **earliest-time (forward)**
pass, whose corresponding row reads `15:12`. `16:06` appears nowhere in the derivation offered for it.
Arithmetic on both is correct (I checked every row of all three boxes; and `23:00 − (15+236+15+118+30) =
16:06` ✓), so this is legibility, not error — but "the chain is the finding" is the board's whole claim,
and the chain shown is not the chain claimed.

**G20 · LOW · Two smaller board defects.** CONFIRMED.
(a) `replay.py:230` composes `t_orsched = t_accept + **20**` while `replay.py:189` budgets the same gap at
**15**. Both are `[D]`, and they disagree — so the board's projection is systematically 5 minutes optimistic
about a gap it invented, which feeds the `margin NARROWED` narrative.
(b) The first held line prints `window margin 240m under the site's own budgets` at the referral boundary —
where no budget chain constrains anything and 240 is simply the OR-block width (`clamp_close − 240`,
`:167-169`). Also `:192`'s `incision to clamp 30m [D]` is refuted by the very case replayed (incision 18:40,
real clamp 21:15 = 155m) and nothing says so.
(c) `measures()` at `:389-391` silently drops referrals with an empty `minutes_to_second_contact` and prints
`n=` the survivors — correct T5 form, but the *number excluded* is never named, in the same block that
proudly prints `NOT DERIVABLE` for the authorization rate.

## 3.5 · Claims, receipts and the public surfaces

**G21 · HIGH · `README.md` publishes two numbers its own "how to check" column refutes.** CONFIRMED.

```
README.md:389  | Tracked files | **133** | `git ls-files \| wc -l` |     →  actual 182
README.md:391  | Conformance   | **51 GREEN · 9 PASS-UNVERIFIED · 0 FAILED** | `python conformance/run.py` |
                                                                          →  actual 53 GREEN
```

The first QC found this row stale by one (133 vs 134) and named it *"the row whose header says re-derive
rather than trusting this file is the one that fails re-derivation."* It is now stale by **49** and by
**2**. `claims.py --surface README.md` returns **GREEN, 4 claims** — battery totals are not among them.

**G22 · HIGH · The claims gate is still blind by form to every long-form battery total, and that is the
half of the fix that was ordered and skipped.** CONFIRMED.

```
$ python -c "…re.findall(pattern, '16 gates · 280+ assertions · 50 GREEN · 9 PASS-UNVERIFIED · 0 FAILED', re.I)"
  battery_green      pattern '(\d+) G ·'      -> []
  battery_unverified pattern '· (\d+) PU'     -> []
  battery_failed     pattern '· (\d+) F\b'    -> []
```

The first QC's Band-1 item 3 read: *"`claims.py`: `re.I` …, **long-form battery patterns**, float-capable
patterns, S-claims derived from tracked results, `not_derivable_here` corrected, `tracked_files` claim
added."* Landed: `re.I` (`claims.py:353`) and the `edr.html` 41→44 surface fix. Not landed: long-form
patterns, float patterns, S-claims, `tracked_files`. Net effect: the page that already used the
abbreviated form is now case-proof, and the two surfaces that use longhand — **README and the one-pager,
the two that are actually wrong** — remain out of reach.

**G23 · HIGH · The one-pager was updated on 2026-08-27 and none of the first QC's four confirmed stale
claims were fixed.** CONFIRMED. `C:/NEW/REGISTRAR_ONE-PAGER_THE-TOTALITY_2026-08-26.md`, mtime `2026-08-27
10:14`, body carrying the full v3/v4/replication narrative:

- `:2` — *"16 gates · **280+ assertions** · **50 GREEN** · 9 PASS-UNVERIFIED · 0 FAILED"* → 396 / 53
- `:119` — *"the chassis is **present but unpinned and unwired** — no `.git`, no assertable SHA"* → pinned
  (7,895/7,895) **and installed** (the battery has a `chassis · installed` GREEN line and `forge/boot.py`
  stands it up in one command)
- `:46` — *"a complete patch and **four that were REFUSED**"* → 22 fixtures, 15 refused
- `:74` — *"One RTX-class card, **24–32 GB**"* — on the **same line** that later records *"the repository
  asked for 24–32 GB while everything was proven on 16"*
- `:74` — the vendor-named BAA/seat-minimum claim commit `5431695` withdrew from the README ("a legal claim
  I had no basis for") is still live.

`edr.html` is in better shape: `unpinned`, `280`, `four that were REFUSED` are all gone, and it carries
`53 G · 9 PU · 0 F` (gate-visible). It still says `24–32 GB` twice (`:1707`, `:2057`), though `:2057`
hedges it correctly.

**G24 · MEDIUM · `CLAIMS.json`'s blind-spot ledger is stale again, in the same place, one revision later.**
CONFIRMED. `conformance/CLAIMS.json` (regenerated at `f8c6923`, so its battery total is current) contains:

> *"the F-PATCH-DELTA verdict — **VOID-BY-AMBIGUITY, dual-printed (0.57 ∥ 0.375) on the surfaces**; the
> answer key is vaulted in internal/ and the prose verdicts in the tracked RESULTS.md are not parseable…"*

The surfaces now dual-print `0.70 / 0.62` and `S = 0.66 ± 0.04 (n=2)`; RESULTS.md's own closing section
withdraws the COVERS headline; VOID is three revisions old. The mechanism is worth naming: this string is a
**literal in `claims.py:derive()`**, so the file that opens *"GENERATED … a hand-edited claims file is the
drift it exists to prevent, wearing the costume of the fix"* faithfully regenerates its one hand-typed
paragraph on every run. *(The rest of that entry is now more accurate than the first QC's correction of it:
`RESULT_1-template-prior.json` is **not** tracked and never was — the first QC's F32 was wrong about that.)*

**G25 · MEDIUM · `REJECTED.md` still teaches thirteen gates.** CONFIRMED, and it was Band-1 item 4 (size S,
"risk of the fix: none — prose").

```
examples/worked/REJECTED.md:223  **Thirteen gates. None of them refuse it.**
examples/worked/REJECTED.md:248  … control, and sets `verify_tls: False`. **Thirteen gates report content.**
rejected/14-silent-partial-UNCAUGHT.json:2  "Every one of the thirteen gates passes this patch…"
rejected/15-credentials-UNCAUGHT.json:2     "All thirteen gates report content."
```

The single change to `REJECTED.md` in the whole delta was `19 → 20 local_variation entries` at `:35`.
`AGENTS.md §1` sends every completing harness to exactly these files as the highest-signal artifact.

**G26 · CREDIT · The mutation claims are derived, not typed.** `claims.py:_mutation_claims()` *runs* the
F-BATTERY-STRENGTH harness on every `--check` and derives `74.8% / 127 / 32` from it. That is the right
instinct — a battery-strength number that a gate fix silently lowers is exactly what should be gated. Two
caveats: it swallows a bare `except Exception: return {}` (a harness that breaks removes three claims
rather than failing), and it puts a ~0.3 s mutation run inside every claims check.

## 3.6 · The two PROPOSED documents, audited as proposals

The framing is honored and I verified it: `git status --porcelain -- SPEC.md ROADMAP.md` is empty and
`SPEC.md`'s last touch is `01ef07c` (2026-08-26 14:25), well before the proposal. In `SPEC_v0.2_PROPOSED.md`
the fenced sections are **byte-exact** — §0, §1, §2b, §3, §6, §7, §8, §10 all hash identical to `SPEC.md`,
so the perception/action fence, the seven prohibitions, the algebra T1–T6, the layer model and the
floor-as-NULL are untouched. **On the smuggling question the SPEC proposal is clean except for one item.**

**G27 · HIGH · `ROADMAP_v2_PROPOSED.md:35` asserts GREEN over a check the battery prints as
PASS-UNVERIFIED.** CONFIRMED.

> *"…chassis pinned 7,895/7,895 and boots in one command · **claims gate holding two public surfaces
> GREEN.**"*

Battery, same tree: `PASS-UNVERIFIED  claims · public surface … no REGISTRAR_SURFACE set — nothing to
compare (set it to a rendered page)`. `CLAIMS.json` records `"battery_configuration": "default — no
REGISTRAR_SURFACE set"`, and `.github/workflows/battery.yml` deliberately does **not** assert it.
`grep -rn "two public surfaces"` returns exactly this one line. Collapsing PASS-UNVERIFIED into GREEN, in a
headline receipt line, in the document that restates the three-state grammar.

**G28 · HIGH · `SPEC_v0.2_PROPOSED.md:127` would make the constitution contradict the shipped schema.**
CONFIRMED. The line appends `holds` to the table whose preamble (`:115-116`, unchanged) reads *"Every
**row** must carry all of the following, or it does not validate."* Measured against
`schema/patch.schema.json`:

```
top-level required:      ['schema_version', 'site', 'rows']        ← holds is OPTIONAL
top-level properties:    [… 'holds']                               ← holds is PATCH-level
$defs/row required:      ['target','value','inverse','evidence','shadow_run','expiry','author']
$defs/row additionalProperties: False                              ← a row carrying `holds` is REFUSED
```

Three errors in one row: wrong level, wrong optionality, and a field that gate 14 would refuse. The
amendment log describes it only as "the spec's table never carried it" and does not flag that it changes
the required-field set.

**G29 · HIGH · `ROADMAP_v2_PROPOSED.md:225` says "Nothing removed as wrong" and one doctrine sentence is
gone.** CONFIRMED.

```
$ grep -c "never the \*\*material" ROADMAP.md plans/ROADMAP_v2_PROPOSED.md
ROADMAP.md:1
plans/ROADMAP_v2_PROPOSED.md:0
```

`ROADMAP.md:290` — *"**escalation** — the resident may escalate the **shape** of a problem, never the
**material**, with every payload constructible from the public seed plus its own prose, checkable"* — is
the only statement of that rule in the tracked non-`internal/` tree. It is a rule about what a resident may
**emit**, i.e. squarely on the boundary `SPEC §8` governs, and adopting v2 as written deletes it from the
repository's public doctrine. (Checked and *not* a loss: "no surface may exist that only makes sense at
`live`" survives byte-exact in `SPEC §2b`.) Also quietly dropped: v1's two named-open clinical provenance
items (viability criteria, per-organ required information, `t_cold_ischemia`), which the battery still
reports open (`replay · guard enforcement … element provenance is TODO-VERIFY`).

**G30 · MEDIUM · Both proposals restate "zero fabrications" without its scope, in the same clause that
names the run that fabricated.** CONFIRMED. `ROADMAP_v2_PROPOSED.md:39` — *"`S = 0.66 ± 0.04 (n=2)` **over
a 0.47 floor**, **zero fabricated values in any run ever**"*, tagged `[M]`. `SPEC_v0.2_PROPOSED.md:751-752`
says the same. The 0.47 floor *is* a run and it fabricated once — I measured it: `19/40 · S = 0.47 ·
fabrications: 1`. Both source texts scope it correctly (`RESULTS.md:525` "three fresh sessions…";
`ROADMAP.md:127` "`S = 0.47 · 1 fabrication`"); the proposals generalise a scoped invariant into a
universal one, in a summary row, under the strongest tag.

**G31 · MEDIUM · Four proposal numbers are stale or wrong.** CONFIRMED.
`ROADMAP_v2_PROPOSED.md:33` `52 GREEN` → 53 (fair: 52 was true at authoring 11:51 and `f8c6923` raised it
at 12:09 while updating `CLAIMS.json`, `ROADMAP.md` and `SPEC_v0.2_PROPOSED.md` but not this file — stale
by eighteen minutes, by a commit that knew) · `SPEC_v0.2_PROPOSED.md:750` *"23-fixture adversarial corpus"*
→ 22, a number the first QC already documented as wrong · both documents say *"six named exposures"* while
the battery **measures 7** · both say *"four documented instances in two days"* against
`logs/SESSION-LOG.md:11` and `battery.yml:3`, which both say one day.

**G32 · MEDIUM · `SPEC_v0.2_PROPOSED.md` asserts R1–R5 built in four places while its own §11 says R2 is
UNBUILT.** CONFIRMED. `:39` — *"STATUS: BUILT AND MEASURED THROUGH R5 of §11; R6 open"*; `:14`, `:20`,
`:784` agree. `:747-749` — *"**R2 PARTIAL, stated precisely: `core/measures/` as a module is UNBUILT** …
the module remains open work"*, and `demo/replay.py:376` prints the same. Verified: `ls core/` has no
`measures`. Compounding, `:25-28` carries a correction note claiming *"The claim is corrected above to
R2-PARTIAL with the receipt named"* — the log item above (`:20`) still reads "R6 named as the only open
rung". **A note asserting a correction that was not made** is the drift class the note is about.

## 3.7 · The first QC's remediation list — what is actually open

Commit `7646f98` is titled **"Phase 1: the instrument batch — every QC mechanical finding closed."**
Measured at `38f9b68`:

| Band-1 item | Status |
|---|---|
| 1 · history decision | **DONE, excellently** (G9). Gate widening + allowlist: **OPEN** (G12), disclosed |
| 2 · rubric v2/v3, abstention guard, floor clause | **DONE and effective** — probe A (0 rows) and probe B (1 row + 19 holds) now both hit `ABSTAINED`. **`both-ways-substring removed`: NOT DONE, and v3 extended it (G2)** |
| 3 · claims.py `re.I` | **DONE**. long-form patterns · float patterns · S-claims · `tracked_files`: **NOT DONE** (G22). `edr.html` 41→44: **DONE**. one-pager's four numbers: **NOT DONE** (G23) |
| 4 · REJECTED.md "thirteen gates" | **NOT DONE** (G25) |
| 5 · exposures line derived, not filename-counted | **DONE, and better than asked** — the battery now runs each fixture and FAILS on a stale label. Exposures count in CLAIMS.json: not added |
| 6 · `edr.yml` null + shared refusal + parse-all-yml test | **DONE, and generalised** — a new battery check sweeps every tracked YAML for None keys. The class is dead, not the instance. |
| Band-2 7 · merge/park fork/battery, record in contracts | **DONE** (`FORKS.md:94`, `worktree.py:72`). Prune `reg-pin`: **OPEN** — still registered |
| Band-2 9 · attest word-boundaries / negation | **NOT DONE** (G5, G15) — `attest.py`'s only change is a yaml guard |
| Band-2 10 · `$holds` schema-typed, `or` made non-silent | **NOT DONE** (G7) |
| Band-2 11 · SPEC §9 tense, banked-status preamble | **NOT DONE in `SPEC.md`** (untouched since `01ef07c`); the proposal fixes it, and a proposal is not a fix |
| Band-2 12 · `require_forge` wired | **DONE** (G16), with the finding ID mis-cited |

**Four Band-1 items are open under a commit that says every mechanical finding is closed** — and two of
them (item 3's long-form patterns, item 4's prose) are the direct cause of live wrong numbers on `README.md`
and the one-pager today.

---

# 4 · WHAT IS RIGHT

**Three strongest design decisions, verified rather than admired:**

1. **The history rewrite was done properly, and "properly" is the hard version.** The purged commits are
   *absent from the object database*, not orphaned; no `refs/original/`, no reflog path back, no pack
   backup; the 68-row map is accurate in every row including the 28 identity rows that make the `87c463d`
   pin claim true; both fork branches were genuinely rebased. Most projects that "rewrite history" leave
   the objects sitting in `.git`. This one did not, and I checked all 40.
2. **The abstention guard actually closes the hole it was built for.** The first QC's probes A and B — the
   ones that reached SHAPED by doing nothing — now both return `ABSTAINED` under v2/v3, and the guard is
   checked *before* every band so no band is reachable by abstention. The threshold (8) was declared before
   any run, with its justification. The fix is mechanical, pre-registered, and it works.
3. **The YAML None-key class was killed, not its instance.** `edr.yml:151` was fixed *and* a new battery
   check now sweeps **every tracked YAML** for a None key (`hygiene · yaml reserved keys … no tracked YAML
   carries a None key`). That is the difference between closing a bug and closing a bug class, and it is
   the pattern the rest of the remediation list should follow.

**Three strongest pieces of evidence discipline:**

1. **`RESULTS.md` withdraws its own headline on the strength of one cheap replication.** `S = 0.70 COVERS`
   was published with its knife-edge stated; run B returned `0.62`; the closing section reads *"the
   single-run COVERS claim is **WITHDRAWN** as a headline"* and replaces it with an interval. It then names
   the *reason* for the variance as a finding about the kit (the row-XOR-hold boundary on split-answerable
   targets is under-specified) rather than as noise. That is a project arguing against its own best number.
2. **Every published falsifier number reproduces exactly, first-hand, against the correct key** — 0.70,
   0.62, 0.55, 0.47, 0.45, 0.38, and `0.375` under v2. Six numbers across three rubrics and two corpora, no
   drift, no rounding games (0.62 is a true 0.625 and is stated as the band boundary it is). The findings
   above are about bookkeeping and matching, not about honesty in the arithmetic.
3. **`PREREGISTRATION_v3.md` opens by naming its own conflict of interest** — *"v3 is designed after two
   candidates exist, by a session that has seen their per-target grades"* — and then lists three specific
   guards, including symmetric re-scoring of every arm. It also records the near-miss: the first v3 run
   printed SHAPED without implementing the pre-registered floor clause, *"the F1 class recurring inside the
   fix for F1, caught before publication."* The `--floor`/`PROVISIONAL` machinery exists because of that
   catch. (G3 is the same class one band over — but the pattern of catching it is real.)

Also worth recording: `F-RESIDENT`'s pre-registration (`38f9b68`, landed during this audit) includes the
clause *"the floor must also beat the cron, or the floor itself is theatre and that prints loudly"* — a
pre-committed funeral for the null, not just for the bet.

*(Calibration note: my first-pass list was roughly twice this length and half of it turned out to be
already recorded in `logs/SESSION-LOG.md`, `RESULTS.md`, `PROVENANCE.md` or a commit message. The §6
novelty rule was enforced against my own draft; what remains above survived it.)*

---

# 5 · REMEDIATION PLAN

Sizes: **S** < 1 h · **M** half-day · **L** day-plus.

## Band 1 — before the next falsifier number is generated or published

| # | Fix | Risk of the fix | Test that proves it | Size |
|---|---|---|---|---|
| 1 | **G1** `score.py` takes `--delta`/`--corpus` and **refuses to run without one**; records `{rubric, floor, corpus, delta_sha256}` in the result; names it `RESULT_<corpus>_<rubric>_<candidate>.json`. Fix `README.md:68-72` to name the *v4* extractor. | none — pure addition; historical numbers are unaffected because they were computed against the correct keys | re-score all six recorded arms and reproduce 0.70/0.62/0.55/0.47/0.45/0.38 exactly; running with the wrong key now errors instead of scoring | S |
| 2 | **G1(d)** Un-ignore the synthetic candidates: `!experiments/F-PATCH-DELTA/*.patch.yml`, commit the four Fairbank patches and their result files. The `*.patch.yml` rule protects *real* site material; these are the corpus's own artifacts. | a future real patch must not ride the exception — scope it to that one directory | a fresh clone can reproduce every published S with one command | S |
| 3 | **G2** Replace two-way containment with normalised equality plus an explicit per-leaf `aliases: []` in the key, in **both** `_leaf_match` and the categorical branch. Re-score every arm symmetrically and publish the delta as an instrument correction beside the existing numbers, never instead of them. | the band may move — that is the point; publish both, per §6 | probe: truth `house_coordinator` / filed `not house_coordinator` must score 0; the four substring-only leaves in run A each re-adjudicated by hand and recorded | M |
| 4 | **G3** `score.py --gates <n>`; print `COVERS (PROVISIONAL — gate result not supplied)` without it, exactly as `--floor` already does for SHAPED. | none | a COVERS printed without `--gates` says PROVISIONAL | S |
| 5 | **G21/G22/G23** Add long-form battery patterns and a `tracked_files` claim to `claims.py`; then fix `README.md:389,391` and the one-pager's five stale claims in one pass. Add `README.md` and the one-pager to a standing `REGISTRAR_SURFACE` list so the battery check stops being PASS-UNVERIFIED by default. | a broadened matcher can cry wolf — anchor each new pattern and pin it in the `conformance/test_claims.py` that is still owed from the first QC | `--surface README.md` FAILS before the edit and GREEN after; same for the one-pager | M |
| 6 | **G5** attest: word-boundary matching plus a ~40-char negation/attribution look-behind, so *"(rev 7 change; formerly the on-call supervisor)"* stops refusing a correct row. Add that exact sentence as a fixture. | under-matching a genuine supersession — keep the vocabulary broad and only the *matching* strict | the SOP-03 probe passes; a genuine *"withdrawn in 2024"* row still FAILS | M |
| 7 | **G4/G6** Add the I3 assertion to `build_site_v4.py`; correct `RESULTS.md:468` to "two of three asserted"; correct `CORPUS_v4_DESIGN.md:37` to 39/40 = 0.975; add both v4 runners to the ledger; move `CORPUS_v4_DESIGN.md` into `internal/` or add it to every do-not-read list; rewrite the experiment README for v4 + v3. | none | a planted volume mismatch makes the builder raise; `grep` for the two runner ids returns hits in `README.md` | S |
| 8 | **G10** Record the `git filter-repo` invocation verbatim in `PROVENANCE.md`; fix "69 commits" → 68; **do the post-rewrite fresh clone** and paste its `rev-list --all --objects \| grep _smoke` output. | none | the clone receipt exists | S |

## Band 2 — before adopting either proposal, and before the next fork wave

| # | Fix | Risk | Test | Size |
|---|---|---|---|---|
| 9 | **G27/G28/G29/G30/G31/G32** Do not adopt either document until: the "two public surfaces GREEN" line is removed or earned; `holds` is moved out of the mandatory-row table; `ROADMAP.md:290`'s escalation rule is carried into v2 (or into `SPEC §8`, where it arguably belongs); "zero fabrications" is scoped; 52→53, 23→22, six→seven, two days→one day; and the R1–R5 header is reconciled with §11's R2-PARTIAL. | adopting as-is puts a schema contradiction and a GREEN-over-PU into the constitution | `claims.py --surface` on both proposals returns GREEN; `grep "never the \*\*material"` hits v2 | M |
| 10 | **G12** Widen `hygiene · no derived source text` to history (`rev-list --all --objects` + path scan) and replace `run.py:682-683`'s extension exemption with an explicit allowlist. Decide what to do about `corpus/citations.json`'s 7,753 verbatim characters — it is the one residual a rewrite can never reach. | none for the gate; the citations question is a judgment call | a planted `corpus/x.md` flips the check red | S + operator |
| 11 | **G13/G14/G15** `pnpm install --frozen-lockfile`; rewrite boot §1's pin sentence to what the pin actually buys; drop "build" from the usage line; make "build forbidden" an actual check (assert `dist/`, `lib/` absent) or demote it out of GREEN; `--status` exits 2 on PASS-UNVERIFIED; the `HTTPError` branch checks `< 500`; emit `_file_digests` so the tamper path can name files. | `--frozen-lockfile` will fail loudly if the lockfile drifts — which is the point | boot `--status` exit code is 2; a hand-corrupted (copied, never in-tree) chassis file makes `--check` name it | M |
| 12 | **G17/G18/G19/G20** Compare the two selftest **streams**, not only the transcripts, and run the second replay in a subprocess; reconcile the 15/20 scheduling gap; scope "every budget above is the site's own tape"; either show the backward chain or restate the marquee as "earliest feasible clamp 22:06, window closes 23:00, margin 54m". | none | a deliberately timestamped percept makes the selftest FAIL | M |
| 13 | **G7** Schema-type `$holds` identically to `holds` (or have gate 15 count it and warn); make `holds` + `$holds` additive or an error, never a silent `or`. | `fairbank.*.patch.yml` must still validate — they are the exemplars | a patch with 1 `holds` + 19 `$holds` reports 20, not 1 | S/M |
| 14 | **G16/G11** Fix the `QC F5` → `QC F15` attributions in `chunk.py:486` and `phi_scan.py:386`; add the commit-map pointer beside the `a1149dc` citations in `README.md` and `AGENTS.md`. | none | `git cat-file -t` on every SHA cited in a front-door doc resolves, or sits next to the map link | S |

## Band 3 — opportunistic

**G25** REJECTED.md + fixtures 14/15 "thirteen gates" → sixteen, with the current battery output quoted (S)
· **G24** derive `not_derivable_here` or date-stamp it, and make `claims.py` fail if its own literal is
older than the newest `RESULTS.md` section (S) · **G20(c)** name the excluded-row count in the
second-contact measure (S) · `git worktree prune` the `…/Temp/reg-pin` tree, open since the first QC (S) ·
delete the stray `nul` file (S) · `_mutation_claims()`'s bare `except Exception` should record
PASS-UNVERIFIED rather than silently drop three claims (S) · `SPEC.md`'s §9 tense and banked-status
preamble, independent of whether v0.2 is adopted (S).

---

# 6 · META

**What I could not verify, and why.** (1) **`C:/REGISTRAR-forkC` was excluded by instruction** — a
concurrent session is working there and its branch moved during this audit (`583a464 → 3dfb23a`); anything
it lands is unaudited by both QCs. (2) **HEAD moved under me** — `f8c6923 → 38f9b68` at 12:18:50; the
battery, suites and surface checks were re-run at `38f9b68` and agree, but the `F-RESIDENT` pre-registration
that landed in that commit got a read and not an audit. (3) **The deployed page** — I audited
`C:/Websites/aorta-site/_upload/edr.html` as a file; deployment parity is unchecked, and that file shows as
modified in its own repository's working tree. (4) **The v4 corpus's realism** — I checked its invariants
arithmetically and read its documents; a planted realism defect in the prose would have escaped me the same
way it escaped the first QC. (5) **Whether the substring matcher actually inverted any published leaf** — I
bounded the exposure (4 substring-only leaves in run A, 2 in run B) but did not hand-adjudicate each one
against the corpus; that is remediation item 3's test, not this report's. (6) **The post-rewrite fresh
clone** — see G10; my evidence is local, and local evidence for a *published* property is the weaker kind.
(7) **`floor/closure.py`'s algorithmic correctness** — accepted on its 27 assertions and the board's
arithmetic, which I did check row by row.

**This QC's own blind spots.** Two sweep dimensions ran through subagents; I re-verified every load-bearing
claim from both with my own commands (the reachability probe, the map's identity rows, `PROVENANCE.md:633`,
the schema's `$defs/row`, `ROADMAP.md:290`, the "two public surfaces" line, the floor's fabrication count),
but their LOW/MEDIUM receipts rest on my plausibility check alone. I read the F-BATTERY-STRENGTH experiment
only through its own documents and the derived mutation claims — the 74.8%/127/32 numbers regenerate on
every `claims.py --check`, which is a good property, and I did not audit `mutate.py`'s mutation set for
whether it is representative. I did not exercise the gates against a runtime, because there still is not
one. And **I read the answer key before I probed the scorer**, which means my probes were designed with
knowledge of the key's shape; the substring finding is structural and survives that, but a reader should
know the order.

**Where the brief misled me — QC of the QC brief.** The brief's own CORRECTIONS appendix already fixed the
six things the first QC found, and my delta instructions superseded §1 and §3 correctly, so the brief did
its job. Three remaining notes:

1. **The corrections appendix is now itself stale in one place.** It says *"the worktrees are `main`,
   `fork/plugins`, `fork/witnesses`"* — there are still **four** registered, because the detached
   `…/Temp/reg-pin` tree was never pruned. It has now survived two QC reports naming it. The appendix
   corrected the count of *branches* and inherited the first QC's uncorrected finding about the *worktree*.
2. **§0.4's contamination clause is under-scoped.** It names `internal/f-patch-delta/DELTA.json`. The vault
   now also holds `DELTA_v1.json`, `DELTA_v4.json`, `build_site_v4.py` and four `RESULT_*.json` files, and
   the *tracked* `CORPUS_v4_DESIGN.md` is a spoiler the brief does not mention at all. A QC that read only
   what §0.4 names would still be contaminated and would not know it.
3. **§0.1's read-only rule collides with the run order it prescribes.** `score.py` writes
   `RESULT_<arm>.json` into `experiments/F-PATCH-DELTA/`, and `demo/replay.py` writes `demo/_out/`. The
   first QC ran the scorer and reported the tree stayed clean — which was true only because the artifact
   was gitignored-or-identical, not because nothing was written. The brief should say: *run the scorer
   through a shim, or accept the artifact and say so.* I took the first option and disclosed it above.

**One process note.** Per §0.4 this session accepted answer-key contamination for a second time, and the
disqualification now covers **both** corpora and all three rubrics. Two eligible-runner slots were spent on
2026-08-27 (run A, run B) without being recorded; the ledger should be updated before the next brief is
written, or the next runner's eligibility is unauditable.

*Written read-only at `38f9b68`. The working tree contained `?? nul` before this QC and contains `?? nul`
plus `qc/QC_REPORT_2026-08-27.md` after it. — the battery's battery, second pass*
